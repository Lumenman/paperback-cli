# Журнал работ: почему лист не читается при `-d 150`

Лабораторный журнал: что мерялось, что вышло и что не вышло. Не документация — документация в README. Начат 2026-09-17.
Исходный вопрос: при `-d 150` и избыточности 1:5 лист A4 не распознаётся уверенно.

---

## 1. Что показало чтение кода

### Печать

| факт | где |
|---|---|
| Блок — 32×32 точки данных, шаг блока `NDOT+3` = 35 ячеек | `src/Printer.c` `Drawblock`, `x=(index%nx)*(NDOT+3)*dx+2*dx+border` |
| Межблочный зазор — 3 ячейки из 35, **8.6% площади листа пустует** | там же |
| В блоке: 90 байт данных + CRC16 + 32 байта RS на 127 → чинит до 16 битых байт из 128 | `Encode8((uchar *)block,block->ecc,127)` |
| Данные XOR-ятся с 0x55/0xAA по чётным/нечётным строкам — чтобы пустой блок не был белым пятном | `Drawblock` |

### Раскладка избыточности (важно, уже сделана грамотно)

`src/Printer.c`, цикл «encode and draw data, group by group»: страница делится на
`redundancy+1` непрерывных полос-«строк», j-й член каждой группы кладётся в j-ю полосу,
плюс поворот колонок на `nx/(redundancy+1)`.

Следствие: **потеря одной полосы целиком = ровно один член каждой группы = восстанавливается.**
Потеря двух полос = два члена = лист мёртв. Обрыв резкий, промежутка нет.

Отдельно: декодер полностью **позиционно-независим** — блок находится по своему полю
`result.addr`, а не по месту на листе (`Decodenextblock`: `blocklist[...].addr=result.addr & 0x0FFFFFFF`).
Значит раскладку на странице можно менять как угодно, не трогая декодер. Идея «перемешать блоки
для устойчивости» уже реализована Олегом, придумывать заново не надо.

### Привязка при декодировании

| факт | где |
|---|---|
| Глобальная модель: один угол + один шаг на весь лист | `Getxangle` / `Getyangle` |
| Подгонка делается в окне **1024×1024 в центре страницы** (`NHYST`) | `Getgridintensity`: `searchx0=centerx-NHYST/2` |
| Позиция блока — линейная экстраполяция от центра | `Decodeblock`: `x0=xpeak+xstep*(posx-blockborder)` |
| Локальный перезахват фазы внутри блока есть — `Findpeaks` по проекциям буфера | `Decodeblock` |
| Гейт: локальный шаг не должен отличаться от глобального больше чем на 1/16 (±6.25%) | `fabs(xstep-pdata->xstep)>pdata->xstep/16.0` |
| Окно поиска блока: `blockborder = max(|xangle|,|yangle|)*5+0.4` → при ровном скане **±0.4 шага блока ≈ ±14 точек** | `Preparefordecoding` |
| Измеренная точная позиция блока вычислялась и **выбрасывалась**: «Save block position for displaying purposes» | `Decodeblock`, `blockxpeak/blockypeak` |
| Обход блоков растровый, соседи не используются | `Decodenextblock` |
| Субблочная подгонка 8×8 px со сдвигом ±1 — зачаток локальной синхронизации, включается только когда блок уже не читается | `SUBDX/SUBDY`, ветка `if (answer==17)` |

**Гипотеза, с которой начинал (оказалась неверной):** глобальная подгонка в центре +
экстраполяция к краям даёт накопленный сдвиг, края листа умирают, теряются две полосы сразу
(верх и низ) — худший случай для раскладки избыточности.

---

## 2. Что реализовано

Ветка `quality-map-and-neighbour-resync`, коммит `712a2c5`.

### `--quality-map` (пункт 0)

`Printqualitymap` в `src/Decoder.c`, вызывается из `Finishdecoding`. Массив `qmap`
(`signed char` на позицию, `-2` = не пробовали) выделяется в `Preparefordecoding`.

Легенда: цифра = блок прочитан, значение = байт починено ECC; `+` = 10 и больше;
`#` = найден, но не прочитан; `.` = растр не найден.

Тот же массив используется для логики второго прохода — одна структура на две задачи.

### Привязка от соседей (пункт 1)

- `Predictorigin(pdata,posx,posy,&x0,&y0)` — предсказывает начало окна поиска по измеренной
  позиции соседа. Возвращает **число** годных соседей (нужно, чтобы отличить блок в гуще данных
  от позиции за краем растра).
- `orgx/orgy` — измеренная привязка каждого блока, сохраняется в `Decodeblock`
  (`x0+xpeak`, `y0+ypeak`, до пересчёта в шаг точки).
- Доверяем только соседям с сошедшимся CRC (`qmap` в диапазоне 0..16). Блок со статусом 17
  часто нечитаем именно потому, что найден не там.
- От соседа берётся **только фаза**, шаг остаётся глобальным: шаг, измеренный по одному блоку,
  слишком шумный, чтобы интегрировать его по листу.
- Второй проход по странице (`pdata->pass`), когда соседи уже известны.

---

## 3. Эксперименты

Скрипты рядом: `warp.py` (синтетическая деформация), `scan.py` (симулятор печати и скана,
нужны numpy и Pillow).

Базовый бинарник для сравнения собирался из HEAD во временный каталог:

```sh
mkdir base && cp -r src include lib Makefile base/
for f in src/Decoder.c src/main.c include/paperbak.h; do git show HEAD:$f > base/$f; done
make -C base
```

### 3.1 Синтетическая деформация протяжки

`warp.py`: `srcy = y + d*h*((y/h)^2 - y/h)` — концы закреплены, локальный шаг гуляет от `1-d` до `1+d`.
Страница `-d 100`, 445 блоков.

| d | HEAD | с изменением |
|---|---|---|
| 0.003 … 0.08 | 445/445 | 445/445 |
| 0.15 | 316/445 | 316/445 |
| 0.30 | 158/445 | 158/445 |

Инструментировал счётчики отказов по четырём гейтам `Decodeblock`:

```
d=0.08  PREDICTED 565, x-nopeak=179 x-step=0 y-nopeak=40  y-step=20,  ystep=104.034
d=0.15  PREDICTED 355, x-nopeak=182 x-step=0 y-nopeak=160 y-step=120, ystep=101.381
```

Предсказание срабатывает сотни раз, но исход не меняет. При d=0.15 глобальный шаг измерен как
101.38 вместо истинных 105, а локальная деформация ±15% вышибает блоки по гейту `y-step`
(±6.25%) — предсказание позиции тут бессильно, отвергается не позиция, а шаг.

**Вывод:** ±15% деформации — это не сканер, это фотография под углом. Реальный планшетник даёт
0.1–0.5%, а окно поиска ±0.4 шага блока (±14 точек) съедает и куда больше.

### 3.2 Симулятор печати и скана

`scan.py`: растекание чернил → перекос → дрейф протяжки → размытие оптики (MTF) → дискретизация
на разрешении скана → шум. Страница: 100 КБ, `-d 150 --image-dpi 600`, скан 300 dpi
(`ratio=2`, 2 пикселя на точку), 1112 блоков.

| искажение | HEAD | с изменением |
|---|---|---|
| blur σ=0.4 px | 1112/1112 | 1112/1112 |
| blur σ=0.6 px | 1112/1112 | 1112/1112 |
| **blur σ=0.8 px** | **22/1112** | **22/1112** |
| blur σ=1.0 px | 88/1112 | 88/1112 |
| blur 0.4 + перекос 0.4° | 1112/1112 | 1112/1112 |
| blur 0.4 + дрейф ±5% | 1112/1112 | 1112/1112 |
| blur 0.4 + шум σ=12 | 1112/1112 | 1112/1112 |
| blur 0.6 + растекание 0.6 | 1112/1112 | 1112/1112 |

**Главный результат всей работы.** Геометрия (перекос, дрейф, шум, растекание) не мешает вообще.
Решает оптическое разрешение, и это **обрыв, а не склон**: между σ=0.6 и σ=0.8 пикселя
лист падает с 1112 блоков до 22. При 2 пикселях на точку запаса по размытию нет никакого.

Немонотонность 22 → 88 при σ=1.0 — это переключение `maxdotsize` на другой размер усреднения,
обе точки одинаково мертвы.

### 3.3 Карта качества различает причины

Замыленный скан (σ=0.8):

```
..+######+#####################+#..
..###############################..
```

Стёртая полоса на листе:

```
..0000000000000000000000000000000..
...................................
...................................
..0000000000000000000000000000000..
```

`#` по всему листу — оптика. `.` полосой — потерянная привязка. Работает как задумано.

---

## 4. Три версии пункта 1 и почему менялись

**v1 — предсказание вместо глобальной сетки всегда.**
Срабатывало 355 раз из ~565, но результат либо тот же, либо **на 1–2 блока хуже**:
930→929, 846→844, 1092→1090. Причина: фаза, взятая от одного соседа, шумнее подгонки,
усреднённой по всему листу. Забраковано.

**v2 — предсказание как резервный путь.**
Глобальная сетка первой; сосед спрашивается только для блока, который провалился
(`answer<0 || answer>=17`). Регрессий нет, при σ=1.0 стало 90 вместо 88.
Цена: чистая страница 1393 мс против 1096 у HEAD (+27%) — за счёт позиций **за краем растра**,
где `answer=-1` и у позиции есть один сосед, так что повторная попытка запускалась впустую.

**v3 (текущая) — плюс требование двух расшифрованных соседей.**
Позиция за краем растра имеет одного соседа, блок в гуще данных — два и больше.
Накладные расходы на чистой странице исчезли. Вместе с ними исчез и выигрыш в 2 блока.

Тайминги (страница 1112 блоков):

| | HEAD | v2 | v3 |
|---|---|---|---|
| чистая страница | 958 мс | 1393 мс | 888 мс |
| массовые отказы | 2273 мс | 5822 мс | 4296 мс |

Удвоение времени на сбойной странице — ровно там, где вторая попытка и нужна.

---

## 5. Честный итог

1. **Пункт 1 не дал измеримого выигрыша ни в одном смоделированном режиме.** Реализован
   корректно, срабатывает, на здоровой странице ничего не стоит, хуже нигде не делает.
   Может помочь на реальной мятой бумаге или на снимке камерой — локальную коробку бумаги
   симулятор не моделирует. Но доказательств пользы у меня нет.
2. **Исходный диагноз («срыв глобальной привязки у краёв») был неверен.** В `Decodeblock`
   уже есть локальный перезахват фазы в окне ±14 точек — на порядок больше, чем даёт
   любой планшетник. Геометрия не узкое место.
3. **Узкое место — оптика.** При `-d 150` и скане 300 dpi на точку приходится 2 пикселя,
   и MTF сканера σ≈0.8 px убивает страницу целиком. Это объясняет и то, почему `-r 5` не
   помогает: отказы не случайные и не локальные, а массовые, ECC в такой ситуации бессилен.
4. **Полезен оказался пункт 0.** Карта — единственное, что отличает «растр потерян» от
   «точки нечитаемы», не гадая.

---

## 6. Что делать дальше

### Сначала — бесплатное

```
paperback-cli --decode --quality-map -i scan.bmp -o out.bin
```

- сплошные `#` → оптика: скан 600 dpi (4 px на точку, включается усреднение 4×4 в
  `Preparefordecoding`: `maxdotsize=4`), печать `--image-dpi 600` вместо умолчания
  `3*dpi=450`, которое лазерник 600 dpi пересемплирует с коэффициентом 1.333;
  в драйвере снять автоуровни, резкость, descreen;
- `.` полосами/по краям → геометрия, тогда v3 начнёт работать;
- много `5`…`9` и `+` → запас ECC тает, лист на грани.

Проверить `-s`: 70 для струйника много (растекание), 60 лучше; для лазера 80.

### Потом — по возрастанию цены

| шаг | цена ёмкости | что чинит |
|---|---|---|
| Клок-гребёнка в межблочном зазоре (3 пустые ячейки уже оплачены) | 0 | абсолютную привязку каждого блока |
| RS поперёк всех блоков страницы вместо XOR 1:N | 0 | обрыв «две полосы = ноль» → плавную деградацию |
| Полосная топология Softstrip (клок по краям полосы 190 мм) | **+8%** (зазор 8.6% → клок 0.2%) | построчную синхронизацию |

Про полосную топологию: построчный клок внутри блока 32×32 при `-d 150` — это клок на
дистанции 5.9 мм, там деформаций нет, платить не за что. Он начинает работать только
если полоса во всю ширину поля.

---

## 7. Воспроизведение

```sh
# страница
paperback-cli --encode -i big.bin -o p150.bmp -d 150 --image-dpi 600

# симуляция скана 300 dpi с размытием оптики
python experiments/scan.py p150.bmp t.bmp ratio=2 blur=0.6
python experiments/scan.py p150.bmp t.bmp ratio=2 blur=0.8 gain=0.5 skew=0.4 noise=8

# синтетическая деформация протяжки
python experiments/warp.py p150.bmp w.bmp 0.15

paperback-cli --decode --quality-map -i t.bmp -o o.bin
```

`scan.py` требует numpy и Pillow. `warp.py` — только stdlib.

---

## 8. 18.09: измеренная оптика вместо угаданной, и дырка в декодере, которую она нашла

Рядом положены исходники `paper-sound` — печать звука гуляющей линией. Формат
другой, интересна метрология: там та же пара «лазерник + планшетник» и лист,
который печатает известную величину и меряет её обратно.

### 8.1 Что взято: PSF, померенный на бумаге

`paper-sound/grating.py` печатает полосу из девяти решёток с известными
периодами и читает контраст каждой — это и есть MTF пары принтер+сканер.
Результат (`LAB.ru.md` §24.6): подгонка `A*exp(-2pi^2 s^2 / lam^2)` даёт
**A = 0.919, s = 1.077 px при 600 dpi**, причём одной гауссианы мало — острое
ядро s ≈ 1.09 плюс широкий ореол, плоский уже к периоду 20 px и стоящий 8%
контраста. Рассеяние тонера и блик стекла.

`scan.py` получил режим `mtf=1`, который ставит вместо угаданного `blur` эти
две гауссианы (`MTF_SIGMA`, `MTF_HALO`). `mtf=` — множитель на сигму, то есть
«во сколько раз сканер хуже померенного». Проверка — `experiments/mtf_check.py`:
девять решёток через симулятор, контраст против непорченого, допуск 0.06.

| период px | модель | бумага |
|---|---|---|
| 2.9 | 0.056 | 0.053 |
| 4.9 | 0.350 | 0.400 |
| 9.9 | 0.731 | 0.690 |
| 19.7 | 0.866 | 0.815 |
| 38.7 | 0.921 | 0.956 |

**Первый же вывод отменяет §3.2.** Угаданный blur=0.8 px на скане 300 dpi — это
1.6 px при 600 dpi, то есть **в полтора раза хуже настоящего сканера**. На
измеренном PSF лист `-d 150` читается целиком и на скане 600 dpi, и на 300 dpi.
Оптика не была стеной, стена стояла на полтора сигмы дальше.

### 8.2 Где настоящий предел (измеренный PSF, множитель сигмы)

Страница 40 КБ, 445 блоков, `--image-dpi 600`. Плотность `-d` округляется до
целых пикселей на ячейку: 150 → 4 px, 200 → 3 px, 300 → 2 px при 600 dpi.

| лист | px на точку в скане | целиком читается до |
|---|---|---|
| `-d 150`, скан 600 dpi | 4 | > 2.0x (не доведено) |
| `-d 150`, скан 300 dpi | 2 | 2.0x, при 2.25x — 413/445 |
| `-d 200`, скан 600 dpi | 3 | 1.75x |
| `-d 200`, скан 300 dpi | 1.5 | не читается вовсе |
| `-d 300`, скан 600 dpi | 2 | 1.0x — запаса нет |

Два пикселя на точку работают, но ровно на померенном сканере; полтора не
работают никогда — это Найквист, а не оптика. Практический ответ на исходный
вопрос: **`-d 150` со сканом 600 dpi имеет двукратный запас по размытию**, и
если лист не читается, виновата не плотность.

### 8.3 Что PSF нашёл в декодере: провал между двумя пикселями

Сначала цифры выглядели как ерунда — немонотонность, та же, что в §3.2
(22 → 88), только резче:

| множитель сигмы, скан 300 dpi | HEAD |
|---|---|
| 1.0 | 445/445 |
| 1.5 | **6/445** |
| 1.75 | **не найден заголовок** |
| 2.0 | 445/445 |

Более размытый лист читается, менее размытый — нет. Размытие так не работает,
значит виноват декодер. Карта качества: при 1.5 весь лист в `#` — блоки найдены
и не прочитаны.

Проверено по порядку:

1. **Не таблица `factor`.** `Recognizebits` вычитает соседей как
   `factor*себя - 4 соседа`, `factor` из {1000, 32, 16} — три жёстких значения,
   два из них на октаву друг от друга. Гипотеза «лист попал между ними»
   красивая и **неверная**: с таблицей {1000, 48, 32, 24, 16, 12} ничего не
   изменилось.
2. **Не оценка резкости и не сетка.** Логировано: `cmin/cmax`, `contrast`,
   `sharpfactor`, `xstep/ystep` — всё меняется гладко и монотонно, шаг измерен
   как 69.885 против истинных 70.0 на всех листах.
3. **Резкость — симптом.** Через временной `PB_SHARP` на листе 1.5x:
   `sharp=0` → 445/445, автоматический `sharp=0.76` → 6/445. Но `sharp=0`
   ломает лист 2.0x (295/445), а `sharp=0.3` ломает его совсем (16/445).
   Ни одного значения, годного для обоих, — значит не в силе фильтра дело.
4. **Фаза сетки.** `Decodeblock` собирает **девять** сеток — со сдвигами ±1 px
   во всех направлениях, — а `Recognizebits` зовёт ровно одну, `g[4]`,
   несдвинутую. Остальные восемь 1.20 использует только для пересборки блока по
   субблокам с максимальной дисперсией. При двух пикселях на точку ошибка фазы
   в один пиксель — это половина точки, и все 32x32 отсчёта берутся не оттуда.

### 8.4 Исправление: три строки

`Decodeblock` перебирает уже собранные девять сеток, пока блок не прочитан:
несдвинутая первая, затем четыре по осям, затем диагонали. Учёт `M_BEST` не
дублируется — цикл обошёл ту же ветку, что и раньше.

| множитель сигмы, `-d 150` скан 300 dpi | HEAD | с циклом |
|---|---|---|
| 1.0 | 445/445 | 445/445 |
| 1.25 | 445/445 | 445/445 |
| **1.5** | **6/445** | **445/445** |
| **1.75** | **мёртв** | **445/445** |
| 2.0 | 445/445 | 445/445 |
| 2.25 | 411/445 | 413/445 |
| 2.5 | мёртв | мёртв |

Кривая стала монотонной: провала между двумя пикселями больше нет. На остальных
двенадцати клетках матрицы (`-d` 150/200/300 x скан 600/300 x сигма 1.0/1.5/2.0)
результат совпал с HEAD до блока — регрессий нет.

Проверено отдельно: восстановленный файл совпадает с исходным по SHA-256
(`--expect`), а страница чистого шума по-прежнему не отдаёт ни одного блока —
перебор сдвигов не выдумывает данных, за ним всё те же RS и CRC16.
`make test` и `test_core` проходят.

Цена:

| | HEAD | с циклом |
|---|---|---|
| чистая страница | 2970 мс | 2443 мс |
| безнадёжная страница | 3024 мс | 7468 мс |

Чистая страница не платит (первой пробуется та же сетка, что и раньше; разница
в пределах шума стенда). Безнадёжная платит 2.5x: восемь лишних `Recognizebits`
на каждый непрочитанный блок, на каждом размере точки. Это та страница, которую
всё равно пересканируют.

### 8.5 Что ещё оттуда стоит взять — по убыванию пользы

1. **Лист печатает собственную линейку** (`LAB` §23.11) — **сделано, §9**. Там штрих печатается
   шириной 4 px, и если скан отдаёт 3.2 px, это физически невозможно: краска
   только растекается. Значит сканер «выпрямляет» края — резкость или
   собственный контраст, — и выбрасывает ровно те пиксели частичного покрытия,
   в которых живёт субпиксельная позиция. Автоуровни и белую точку так видно
   **из самого изображения, без эталона**. У нас точка печатается известной
   ширины (`-s 70` от шага, при `-d 150` и 600 dpi это 2.8 px) — приём годится
   один к одному и ложится рядом с `--quality-map`: карта говорит
   «не прочитано», линейка — «потому что сканер порезал края».
2. **Порядковая статистика вместо порога** (`axis.py`, §23.1) — **померено и отклонено, §10**. Монотонная
   тоновая кривая переставляет уровни серого, но не может их переупорядочить,
   поэтому ранг переживает гамму, автоуровни и экспозицию, а значение — нет.
   У нас порог считается из `cmin/cmax` по 3% хвостам гистограммы **центрального
   окна 1024x1024** — это значения, а не ранги. В `paper-sound` идея померена
   и **отклонена**, но там оценивалась субпиксельная позиция по одному ряду;
   у нас нужно бинарное решение по 1024 точкам блока, и запас совсем другой.
3. **Профиль, просуммированный вдоль наклона** (`read_tracks.col_profile`,
   §19) — **уже был в 1.20; нашлось другое, §11**. Сдвиг каждой строки на -lean*y индексной арифметикой перед
   суммированием, ни одного пересчитанного пикселя, двадцать строк кода — и
   потолок по перекосу уехал с 0.33° до 16°, то есть вышел из ридера и упёрся
   в стекло. У нас `Getxangle`/`Getyangle` ищут угол честно, но в окне
   `NHYST` 1024x1024 в центре листа; лист мерится своей серединой, а не целиком.
   Дёшево, но не нужно, пока §3.2 показывает, что геометрия не жмёт.
4. **Не брать: решётка вместо одной точки** (§24.6, +8 dB). Выигрыш там из
   метрологии — «краёв x ход края в квадрате», — и он про оценку *позиции*
   штриха. У нас носитель бинарный, позиция ничего не несёт, переносить нечего.

### 8.6 Воспроизведение

```sh
paperback-cli --encode -i small.bin -o e150.bmp -d 150 --image-dpi 600

# скан 600 dpi и 300 dpi с измеренным PSF
python experiments/scan.py e150.bmp s.bmp ratio=1 blur=0 mtf=1
python experiments/scan.py e150.bmp s.bmp ratio=2 blur=0 mtf=1.5

paperback-cli --decode --quality-map -i s.bmp -o o.bin -f
python experiments/mtf_check.py      # PSF против бумаги
```

---

## 9. 18.09: линейка — ширина точки в скане

Пункт 1 из §8.5. Точка печатается известной долей ячейки, а краска на бумаге
только растекается: пройдя принтер, бумагу и стекло, точка может вернуться
шире, но не уже. Значит лист несёт собственную линейку, и по ней видно
единственное повреждение, которое больше нигде в изображении следа не
оставляет — сканер, «выпрямляющий» края.

### 9.1 Чего в нашем формате нет: изолированной точки в смысле `paper-sound`

Там штрих шириной 4 px стоит в полосе шириной 38.7 — вокруг него бумага.
У нас точка занимает `-s` процентов ячейки, по умолчанию 70, и при `-d 150`
с `--image-dpi 600` это **3 px из 4**: между двумя соседними чёрными точками
остаётся 1 пиксель бумаги. Первая версия искала точку, вокруг которой бумага,
и не нашла ни одной.

Нашлась она там, где и должна: изолирована не точка, а **ячейка**. Белая ячейка
— это 4 px бумаги целиком, потому что точка в ней не напечатана. Поэтому
критерий — «чёрная ячейка, у которой все четыре соседние ячейки светлее на
(cmax−cmin)/4», и такие на случайных данных составляют около 3% ячеек.

### 9.2 Бины не работают, интерполяция работает

Профиль набирается с шагом в 1/24 ячейки. Первая версия складывала в бины по
тому, куда попали целые пиксели, — и бины остались пустыми. Причина ровно
обратна той, на которой стоит `grating.py`: там периоды решёток **нарочно**
не целые, чтобы штрихи прошли все субпиксельные фазы. У нас лист напечатан и
отсканирован на одном и том же dpi, шаг сетки — почти ровно целое число
пикселей (69.885 на 35 ячеек, то есть 1.9967 px на точку), и точки со своей
фазы не сходят. Ждать, что они разбредутся по бинам, нечего.

Поэтому профиль **берётся в точках**, а не раскладывается по ним: значение
буфера в дробной позиции, билинейно по четырём пикселям вокруг. Тогда каждый
бин кормится каждой точкой, и разрешение профиля не зависит от того, куда сел
растр. Билинейность по обеим осям, а не ближайшая строка: при точке в 3 px срез
на полпикселя мимо центра читается заметно уже.

### 9.3 Что получилось

Ширина на половине собственной глубины, в процентах ячейки, по обеим осям в
один профиль. Меряется по **неразмытому** буферу блока (`pdata->unsharp`) —
тому, что был до unsharp-маски декодера, — по первым 400 изолированным точкам,
то есть примерно по тринадцати блокам.

Цифровой лист, бумаги не видевший:

| лист | напечатано | линейка |
|---|---|---|
| `-d 100`, `--image-dpi 300`, `-s 70` | 2 px из 3 = 67% | **67%** |
| `-d 150`, `--image-dpi 600`, `-s 70` | 3 px из 4 = 75% | **75%** |
| `-d 100`, `-s 50` | 2 px из 3 = 67% | **67%** |
| `-d 100`, `-s 100` | 3 px из 3 = 100% | **100%** |

Точно, включая округление принтера до целых пикселей — то самое, которого
декодер знать не может, потому что не знает `--image-dpi`. Поэтому порог
предупреждения широкий: **0.9 от номинала `-s`**, а не 0.95, как в
`paper-sound`, где печатная ширина известна ровно.

Через измеренный PSF (§8.1) и через порчу тоновой кривой:

| скан | линейка | предупреждение |
|---|---|---|
| измеренный PSF, 600 dpi | 84% | — |
| измеренный PSF, 300 dpi (2 px на точку) | 102% | — |
| он же, линейное растяжение уровней | 84% | — |
| он же, белая точка 10% | 77% | — |
| он же, белая точка 20% | 66% | **да** |
| он же, S-кривая ×1 | 72% | — |
| он же, S-кривая ×2 | 60% | **да** |
| он же, S-кривая ×3 | 49% | **да** |

Размытие уводит ширину **вверх** (75 → 84 на 600 dpi, до 102 на 300 dpi, где
FWHM определяется уже блюром, а не печатной точкой) — правило «только шире»
держится, и линейка никогда не обвиняет зря. Линейное растяжение уровней не
двигает ничего: половина глубины берётся от собственных минимума и максимума
профиля, и к линейному отображению она инвариантна. Видна только **кривизна** —
это ровно то, что §23.11 проверяет своим `3x − 1.0`.

### 9.4 Где печатается

Строка с измерением — под `--quality-map`, рядом с картой блоков: карта
говорит «не прочитано», линейка — «потому что сканер порезал края».
Предупреждение печатается **всегда**, без флага: молчащий испорченный скан —
как раз тот отказ, который оно ловит.

```
Dot measures 84% of the cell against 70% printed (-s, which the printer rounds
to whole pixels); 400 dots
```

Проверка в `test_cli.py`: лист `-s 50` при `--dpi 100` обязан прочитаться как
67% (арифметика принтера, без всякого скана); он же, представленный декодеру
как `-s 100`, обязан вызвать предупреждение; лист `-s 100` — прочитаться как
100% и промолчать. Ни numpy, ни картинок — только сам бинарник.

Стоимость: 400 точек на страницу, примерно тринадцать блоков, на чистой
странице в шуме стенда (2722 мс против 3021 у HEAD).

### 9.5 Что линейка не ловит

- **Линейные правки** — яркость, контраст «ползунком», растяжение уровней.
  Они не двигают половину глубины и данным не вредят.
- **2 px на точку.** Там ширина определяется размытием (102%), и запаса на
  «уже, чем напечатано» почти нет — предупреждение сработает, но поздно.
  Линейка полезна там, где полезно и всё остальное: 4 px на точку.
- **Чужой `-s`.** Если лист печатался не с умолчанием, декодеру надо это
  сказать (`-s` работает и при `--decode`), иначе линейка мерит против чужого
  номинала. Сообщение об этом говорит прямо.

---

## 10. 18.09: порядковая статистика вместо порога — сделано, померено, отклонено

Пункт 2 из §8.5. Результат отрицательный, код удалён; здесь то, что от него
осталось полезного.

### 10.1 Сначала — §8.5 был неточен

«Порог считается из `cmin/cmax` по 3% хвостам гистограммы — это значения, а не
ранги» неверно: 3% хвост гистограммы **и есть** ранг. Претензия должна была
звучать иначе — `cmin/cmax` берутся из **центрального окна 1024×1024** и
применяются ко всему листу.

Настоящий порог живёт не там. В `Recognizebits` он такой:

```
c = grid[j][i]*factor - (четыре соседа)
limit = (сумма c по 1024 ячейкам)/1024 + lcorr*factor
бит = (c < limit)
```

То есть **среднее** по блоку плюс один из трёх сдвигов. Вот это — значение.

### 10.2 Что построено

Порог по рангу: копия `grid1`, `qsort`, `limit = (p[256] + p[768])/2` — середина
между квартилями собственных 1024 ячеек блока. Квартили, а не медиана: доля
чёрных ячеек лишь примерно половина, и медиана при перекосе садится внутрь
одного из облаков, а середина между квартилями остаётся в зазоре.

Добавлено тремя комбинациями (по одной на каждый `factor`) в тот же перебор,
что уже отбирается по CRC, плюс два временных ключа `PB_NORANK` / `PB_ONLYRANK`,
чтобы мерить три режима одним бинарником: только среднее, только ранг, оба.

### 10.3 Померено: ничего, нигде

28 случаев — гамма 0.3…4.0, S-кривые, белая точка, шум, градиент освещения
поперёк листа, тень от корешка до 0.15 яркости, сильное растекание чернил,
и всё это на 4 px и на 2 px на точку, при множителе PSF 1.0, 2.0 и 2.25.

| случай | среднее | ранг | оба |
|---|---|---|---|
| PSF 1.0, 300 dpi | 445/445 | 445/445 | 445/445 |
| PSF 2.25, 300 dpi | 413/445 | 411/445 | 413/445 |
| + гамма 0.4 | 409/445 | 409/445 | 409/445 |
| + гамма 3.0 | 415/445 | 415/445 | 415/445 |
| + градиент до 0.45 | 404/445 | 404/445 | 404/445 |
| тень до 0.15, 600 dpi | 441/445 | 441/445 | 441/445 |
| тень 0.15 + гамма 0.5 | 384/445 | 384/445 | 384/445 |
| растекание 0.9 | 445/445 | 445/445 | 445/445 |

Совпадение до блока во всех случаях кроме одного, где ранг **на два блока
хуже**. Обе колонки одинаково реагируют на всё, что портит лист, — значит
порог в этих отказах не участвует вовсе.

### 10.4 Почему — механизм, а не невезение

Данные XOR-ятся с 0x55/0xAA по чётным и нечётным строкам (`Drawblock`). Это
отбеливание: любой блок, включая блок из одних нулей, выходит на бумагу
примерно **половина на половину**, разброс биномиальный, ±16 точек из 1024.

При гарантированном делении 50/50 среднее по 1024 ячейкам — несмещённая оценка
той самой точки, где кончается одно облако и начинается другое. То есть среднее
здесь **не значение, а оценка ранга**, и оценка по тысяче отсчётов вместо двух.
Порядковая статистика оценивает ту же величину с дисперсией во много раз выше —
отсюда и те два потерянных блока.

Довод из `paper-sound` («монотонная кривая переставляет значения, но не может их
переупорядочить») верен и здесь. Просто Олег уже заплатил за ранг заранее — маской
0x55/0xAA, — и второй раз платить не за что. В `paper-sound` отбеливания нет и
быть не может: там носитель — позиция штриха, а не бит.

### 10.5 Заодно выяснилось, где на самом деле ломается тень

Лист «тень до 0.15 + гамма 0.5» теряет 61 блок. Карта качества:

```
.........000000000000000000000000..
.........000000000000000000000000..
```

Потерянные блоки — **точки, а не решётки**: растр в тени не найден вовсе.
Это `Getxangle`/`Findpeaks`, а не порог; никакое правило выбора порога тут
помочь не могло, потому что до порога дело не доходит. Карта качества опять
отработала как задумано — сказала, куда не надо смотреть.

### 10.6 Итог

Код удалён. Осталось это описание и набор искажённых листов, на которых теперь
меряется всё остальное.

Открытым остаётся другое: `cmin/cmax` действительно сняты с центрального окна и
применяются ко всему листу — это и порог обрезки в unsharp-маске
(`max(cmin,min(...,cmax))`), и оценка резкости, и подстановка `cmax` за краем
блока. На листе с тенью они заведомо неверны для тёмного края. Но пока такой
лист теряет блоки **до** этого места, чинить надо поиск растра, а не уровни.

---

## 11. 18.09: наклонный профиль — он уже есть, а не хватало не наклона, а охвата

Пункт 3 из §8.5. Вышло не то, что предполагалось, и нашлось другое.

### 11.1 Наклонный профиль в 1.20 уже реализован

`Getxangle` перебирает углы `a` и для каждого набирает профиль колонок со
сдвигом каждой строки:

```c
x = x0 + (y0+j)*a/NHYST;               // Affine transformation
```

Это построчная индексная арифметика, ни один пиксель не пересчитывается — то
есть буквально `read_tracks.col_profile(ink, lean)` из §19 звукового журнала.
Переносить нечего: Олег сделал это в 2007 году.

Диапазон: `a` от −(NHYST/20)·2 до +(NHYST/20)·2, то есть ±0.1 рад = **±5.7°**,
шагом 2/1024 = 0.0020 рад = 0.11°. А физический предел — стекло: A4 на A4
перестаёт влезать около 1.2° (`LAB` §18.4). Расширять тоже нечего.

### 11.2 Померено: угол и так измеряется верно

Лист `-d 150`, измеренный PSF, `skew` в симуляторе:

| перекос | должен быть, рад | измерено | прочитано |
|---|---|---|---|
| 0° | 0 | 0.00000 | 445/445 |
| 0.1° | 0.00175 | 0.00195 | 445/445 |
| 0.2° | 0.00349 | 0.00391 | 445/445 |
| 0.5° | 0.00873 | 0.00781 | 445/445 |
| 1.0° | 0.01745 | 0.01758 | 445/445 |
| 2.0° | 0.03491 | 0.03516 | 445/445 |

Всё в пределах собственной дискретности 0.0020 рад. То же на скане 300 dpi и
при множителе PSF 2.0–2.25: угол правильный, шаг правильный (140.00 против
истинных 140), теряются только те блоки, где точки сами по себе нечитаемы.
Наклон — не узкое место, §3.2 опять прав.

### 11.3 Зато нашлось настоящее: вся привязка живёт в одном окне 1024×1024

`Getgridintensity` ставит окно в центр растра, и из него берётся **всё**:
уровни чернил и бумаги, оценка резкости, и — через `searchx0..searchy1` —
профиль, по которому ищутся угол и шаг.

Достаточно, чтобы это окно оказалось пустым. 1024 пикселя при 600 dpi — это
**4.3 см**: наклейка, кофейное кольцо, белый залом, стикер с подписью. Тогда
`cmax-cmin<1`, и лист отвергается целиком:

```
No image
No readable file header found
```

Проверено: вырезать белым ровно окно (с полем 60 px) — и **HEAD теряет лист
целиком** при том, что остальные 95% листа в порядке.

### 11.4 Исправление в две части

**Профиль кормится всей высотой растра.** `h[]` шириной 1024 колонки, и это
остаётся; но строки, которые в него складываются, берутся от `gridymin` до
`gridymax`, а не из того же квадрата. Тех же 256 строк (`ystep=dy/256`), только
размазанных по листу, — цена ровно та же. Два следствия: неверный угол теперь
смазывает гребёнку по всей странице, а не по тысяче строк (1.20 не случайно
пришлось добавлять здесь смещение в пользу нуля — «weights for a=0 and ±2 are
the same»), и пустое пятно **разбавляет** гребёнку вместо того, чтобы быть всей
гребёнкой. Симметрично для `Getyangle` по ширине.

**Уровни: окно, а лист — если окно непредставительно.** `Getlevels` вынесена
отдельной функцией, вызывается дважды: по окну и по растру со шагом строк
(1024 строки, цена не растёт с размером листа). Если контраст окна **меньше
половины** контраста листа, берутся уровни листа.

Сравнение с листом, а не с константой: на здоровой странице оба совпадают, и
тогда всё остаётся ровно как в 1.20.

### 11.5 Почему уровни переключаются, а не берутся с листа всегда

Потому что всегда — хуже. `cmin/cmax` — это 3% перцентили, то есть они зависят
от того, сколько чернил в измеряемой области; на растре доля чернил другая, чем
в окне, и разность `cmax−cmin` уезжает (149/244 → 137/242 на одном из листов,
размах +10%). А `cmax−cmin` стоит в числителе оценки резкости
`sharpfactor=(cmax−cmin)/(2·contrast)−1` — той самой ручки, про которую §8.3
показала, что она на ножевом краю.

Померено, «всегда с растра»:

| лист | окно (1.20) | всегда растр |
|---|---|---|
| PSF 2.0, 300 dpi | 445/445 | **444/445** |
| PSF 2.25, 300 dpi | 413/445 | **409/445** |
| тёмная клякса, 300 dpi | 445/445 | **439/445** |
| тень 0.15 + гамма 0.5 | 384/445 | **414/445** |

То есть широкие уровни реально помогают листу с тенью (+30 блоков) и реально
мешают размытому. Переключение сохраняет все старые цифры и всё равно спасает
мёртвые листы; блендить пришлось бы с перенастройкой резкости, и это отмечено
в коде как `ponytail:`.

### 11.6 Результат

| лист (пятно ровно в окне) | HEAD | с изменением |
|---|---|---|
| 600 dpi, пятно 1144×1144 | **No image** | 389/445 |
| 600 dpi, пятно 1544×1544 | **No image** | 334/445 |
| 300 dpi, пятно 1144×1144 | **No image** | 201/445 |
| 300 dpi, пятно 1544×1544 | **No image** | мёртв |

Последний честно мёртв: 1544 px из 3508 — это 44% высоты листа, и стёрта в том
числе метка страницы, без которой собирать нечего. Остальные три — из «ничего»
в «почти всё».

Регрессий нет: 68 листов набора (гамма, S-кривые, белая точка, шум, градиент,
тень, растекание, перекос 0…2°, плотности 100…300, сканы 600 и 300 dpi,
множители PSF 1.0…2.25) читаются блок в блок как раньше. `make test`,
`test_cli.py` и `test_core` проходят.

### 11.7 Что осталось

Профиль по-прежнему шириной 1024 пикселя и стоит в центре — окно можно
разбавить пустотой, но нельзя сдвинуть. Пятно, накрывающее центральные 1024
колонки **по всей высоте**, гребёнку убьёт по-прежнему. Лечится скольжением
окна, то есть перебором позиций, — но такого повреждения (вертикальная полоса
шириной 4 см через весь лист) у меня в наборе нет, и придумывать его, чтобы
оправдать код, — ровно то, чего не надо делать.

---

## 12. 18.09: три листа одного файла, один принятый неверный блок и 16 бит под ним

Пришло с бумаги: один файл напечатан трижды (`-s 55`, `70`, `85`), три скана
декодируются вместе, программа говорит «910/910, 0 missing, 95 repaired» —
и тут же «File checksum mismatch», файла нет.

### 12.1 Сначала — эксперимент с `-s` был не тем, чем казался

Точка рисуется целым числом пикселей внутри целой ячейки. При `-d 150` и
`--image-dpi` по умолчанию (450) ячейка — 3 px:

| `-s` | нарисовано |
|---|---|
| 55 | 2 px из 3 = 67% |
| 70 | 2 px из 3 = 67% |
| 85 | 3 px из 3 = 100% |
| 100 | 3 px из 3 = 100% |

Битмапы `-s 55` и `-s 70` **побайтово совпадают**; `-s 85` и `-s 100` — тоже.
То есть три листа — это два варианта, а не три. Линейка на сканах подтвердила
независимо: 55 и 70 померены в одни и те же 91%, 85 — в 117%.

| лист | линейка | прочитано | `+` (на пределе ECC) | `#` |
|---|---|---|---|---|
| 55 | 91% | 745/910 | 728 | 269 |
| 70 | 91% | 733/910 | 745 | 283 |
| 85 | **117%** | **890/910** | 826 | 112 |

Крупная точка выигрывает вчистую, и 117% — то есть точки смыкаются, зазора нет
вовсе. Симуляция §«дот-сайз» обещала обратное; она была неправа потому, что её
«крупная точка» — резкая заливка ячейки, а на бумаге это 3-пиксельная точка,
расплывшаяся за её край. Листу не хватает контраста (размах чернила-бумага 135
уровней из ~190), а не зазора, и лишние чернила дают именно контраст.

Энкодер теперь печатает, что нарисовал: `cell 3x3 px, dot 2x2 px = 67% of the
cell`. Без этой строки такой опыт ставится вслепую.

### 12.2 Потом — настоящая находка: принят неверный блок

Сверка по сборкам на тех же трёх сканах (эталон — вывод сборки до
экспериментов):

| сборка | repaired | результат |
|---|---|---|
| HEAD до работ | 90 | файл верный |
| + перебор девяти сеток | 93 | **File checksum mismatch**, файла нет |
| + привязка по листу | 93 | файл верный (везение) |

`--force` на средней сборке даёт файл, отличающийся от верного ровно в **одном
блоке из 910** — блок 182, 49 из 90 байт. Значит блок был принят с неверными
данными, а CRC16 файла это поймал. Это правильная работа последней линии
обороны, но вопрос — как блок прошёл свою собственную проверку.

### 12.3 Механизм: путь со стираниями тратит всю избыточность

В `Recognizebits` после неудачи обычного декодирования есть повтор, которого
**в 1.20 нет** (там только `Decode8(result,NULL,0,127)`; добавлено в этом порту
до моих работ): 32 наименее надёжных байта объявляются стираниями, и RS решает
относительно них.

Рид-Соломон здесь исправляет 16 байт неизвестной позиции или 32 известной. Объявив
32 стирания, мы расходуем **всю** избыточность: любые оставшиеся 95 байт дают
формально корректное кодовое слово. Никакой проверки, кроме **CRC16**, не
остаётся — 1 попытка из 65536 проходит с выдуманными данными. И адрес тут не
спасает: он берётся из того же блока, поэтому всегда правдоподобен и
`Addblock` его пропускает.

Значит цена — число попыток. Перебор девяти сеток умножил его **в девять раз**:
на трёх сканах это ~180 тысяч попыток со стираниями против ~20 тысяч, то есть
ожидание ложных приёмов выросло с 0.3 до ~2.7. Наблюдался один.

### 12.4 Исправление: стирания только на несдвинутой сетке

Сдвиги нужны, чтобы найти **фазу**; с правильной фазой блок читается обычным
путём, где RS остаётся полноценной проверкой. Поэтому `Recognizebits` получила
параметр `erasures`, и восемь сдвинутых сеток идут без стираний — ровно та
экспозиция, что была в HEAD.

Выигрыш сдвигов уцелел полностью, и это не компромисс, а улучшение:

| лист | без гейта | с гейтом |
|---|---|---|
| 02.bmp | 107 пропусков | **105** |
| 85.bmp | 20 | 20 |
| 70.bmp | 177 | 176 |
| 55.bmp | 165 | 165 |
| три скана вместе | 93 repaired, один блок неверен | 92 repaired, **файл верный** |
| синтетика, множитель PSF 1.5 и 1.75 | 445/445 | 445/445 |

Почему стало **лучше**, а не так же: ложно принятый блок не только портит
данные, он занимает слот. `Addblock` проверяет `datavalid[i]!=1`, поэтому
правильный блок, пришедший потом с другого скана, отбрасывается. Одна выдумка
стоит двух блоков.

### 12.5 Что осталось открытым

1. **Путь со стираниями сам по себе — ставка 1:65536**, и на несдвинутой сетке
   он по-прежнему пробуется на каждом размере точки и каждой из 12 комбинаций
   порога. На плохом листе это ~7 тысяч попыток, то есть ожидание 0.1 ложного
   приёма за прогон. Это поведение HEAD, и я его не менял, но теперь известно,
   чем оно оплачено.
2. **CRC16 файла — тоже 16 бит.** Здесь он сработал, но один раз из 65536 не
   сработает, и тогда испорченный файл сохранится молча. Единственное
   доказательство — `--expect` с SHA-256; на листе он печатается в подвале при
   `--header`.
3. **Карта качества не отличает блок, принятый по стираниям.** `answer`
   зажимается в 16, так что такой блок выглядит как обычный «плохой, но
   прочитанный». Отдельный символ для него сказал бы «этот блок держится на 16
   битах» — стоит сделать, это диагностика, а не поведение.

---

## 13. 18.09, вечер: пять напечатанных листов, и решило разрешение печати

Продолжение §12. Напечатаны ещё два листа — с `--image-dpi 600`, которого не
было у первых трёх.

| лист | печать | нарисовано | линейка | прочитано | починено ECC |
|---|---|---|---|---|---|
| 55 | 450 dpi | 2 px из 3 = 67% | 91% | 745/910 | 60 |
| 70 | 450 dpi | 2 px из 3 = 67% | 91% | 733/910 | 60 |
| 85 | 450 dpi | 3 px из 3 = 100% | 117% | 890/910 | 75 |
| **75** | **600 dpi** | **3 px из 4 = 75%** | **96%** | **910/910** | **0** |
| 100 | 600 dpi | 4 px из 4 = 100% | 122% | 908/910 | 38 |

`75.bmp` прочитан **целиком и без единой починки ECC**, и его SHA-256 совпадает
с исходником побайтово. Избыточность 1:5 на этом листе не понадобилась вовсе —
то есть запас есть, и его можно тратить на ёмкость.

### 13.1 Вывод §12.1 был неполон

Там сказано «крупная точка выигрывает вчистую». Пять точек говорят иначе:
выигрывает не крупная точка, а **печать в родном разрешении принтера**. При
`--image-dpi` по умолчанию лист печатается в 450 dpi, и принтер 600 dpi
пересемплирует его с коэффициентом 1.333 — размазывая каждый край ещё до
бумаги. При 600 dpi печать идёт пиксель в пиксель.

По измеренной ширине оптимум виден и он **чуть ниже 100%**:

| линейка | прочитано |
|---|---|
| 91% | 733, 745 |
| **96%** | **910, 0 починок** |
| 117% | 890 |
| 122% | 908 |

Мало чернил — не хватает контраста; слишком много — точки смыкаются и
модуляция гибнет. 96% попадает между. Прежний мой совет «целиться в 75–85%
измеренной ширины» неверен: 91% уже слишком мало.

Оговорка, которую надо держать: у `-s 85` при 450 dpi и `-s 100` при 600 dpi
физическая точка **одна и та же** (3/450″ = 4/600″ = 1/150″), а линейка дала
117% и 122%, блоков 890 и 908. Значит разброс от отпечатка к отпечатку у этого
принтера заметен, и одиночные листы нельзя сравнивать до последнего блока.

### 13.2 Что из этого следует для рекомендаций

1. **`--image-dpi` = разрешение принтера.** Это единственная перемена, после
   которой лист стал читаться идеально. Умолчание `3*-d` (450 при `-d 150`)
   выбрано под общий случай и на лазернике 600 dpi работает против себя.
2. **Линейка — прибор для наводки.** Цель — измеренная ширина чуть ниже 100%.
   Ниже 95% — мало чернил, выше 110% — точки смыкаются.
3. **`-s` — вторичен**, и при `--image-dpi 600` у него всего четыре шага
   (50/75/75/100). `-s 75` при 600 dpi и есть найденная точка.
4. Ноль починок ECC на `75.bmp` значит, что на этой паре принтер+сканер есть
   запас: `-d 200` при `--image-dpi 600` даёт 3 px на ячейку и ~1.8x ёмкости,
   и по §8.2 у трёх пикселей на точку остаётся 1.75x запаса по размытию.

---

## 14. 18.09, ночь: `-d 200` на этом принтере невозможен, и почему `-r 0` — правильный ответ

### 14.1 Растекание — фиксированная величина, а не доля

`ScanImage50.bmp`, напечатан `-d 200 --image-dpi 600`, не читается **вообще** —
ни одного блока, метка страницы не найдена. Линейка объясняет почему:

| лист | ячейка в скане | нарисовано | линейка | измеренное растекание |
|---|---|---|---|---|
| `75.bmp`, `-d 150` | 3.94 px | 3 px = 76% | 96% | +0.83 px |
| `ScanImage50`, `-d 200` | 2.95 px | 2 px = 67% | **99%** | +0.95 px |

Растекание одинаковое — около **0.9 px при 600 dpi, то есть 0.04 мм**. Это
свойство пары принтер+стекло, и оно **абсолютное**, не пропорциональное. Значит
зазор, который нужно оставить, тоже абсолютный, и чем мельче ячейка, тем большую
её долю он съедает. При ячейке 3 px и точке 2 px зазор равен 1 px — растекание
съедает его целиком, точки смыкаются, модуляции нет, читать нечего.

При `-d 150` ячейка 4 px, зазор 1 px, растекание те же 0.9 — и лист читается
целиком без починок. Обрыв между 96% и 99%, то есть между «зазор 0.16 px» и
«зазора нет». Бритва.

### 14.2 Спасти `-d 200` на этой машине нечем

При ячейке 3 px округление `-s` даёт всего два варианта: `-s 50…83` → 2 px,
`-s 84…100` → 3 px. Меньшая точка уже использована. Один пиксель потребовал бы
`-s ≤ 49`, чего CLI не принимает (нижняя граница 50), да и 1 px при 600 dpi —
это 0.042 мм тонера, ниже того, что лазер укладывает повторяемо. Пробовать
можно, рекомендовать нельзя, границу молча опускать не стал.

Значит потолок этой пары — **`-d 150 --image-dpi 600`**, и ёмкость приходится
брать не плотностью, а избыточностью.

### 14.3 Поэтому `-r 0`

Блоков данных на листе A4 при `-d 150 --image-dpi 600`:

| | блоков | байт | к умолчанию |
|---|---|---|---|
| `-r 2` | 948 | 85 КБ | −20% |
| `-r 5` (умолчание) | 1180 | 106 КБ | — |
| `-r 10` | 1280 | 115 КБ | +8.5% |
| **`-r 0`** | **1422** | **128 КБ** | **+20.5%** |

### 14.4 Как сделана раскладка без групп

Формат менять не пришлось: адрес блока данных всегда несёт ноль в старшей
тетраде — там, где блок восстановления несёт размер своей группы. Поэтому
страница без блоков восстановления читается любым декодером, включая 1.20, и
`Finishpage` просто никогда не находит группу для починки (у него уже стоит
`if (pf->ngroup>0 …)`).

Изменилась только раскладка в `Printnextpage`: при `redundancy==0` нет строк,
которые надо возглавлять суперблоком, поэтому метки **разбросаны по листу**, а
данные занимают всё остальное. Метки четыре (`NSUPER`) — при `r>0` их
`redundancy+1` плюс все незанятые ячейки, и не зря: страница, у которой
единственная метка повреждена, теряется целиком. Это ровно то, что случилось с
`ScanImage50`. Четыре метки стоят 0.2% ячеек.

Проверено: стёртый угол с первой меткой не мешает странице назвать свой файл —
теряются только четыре затёртые ячейки.

`-r 1` отвергается: один блок восстановления на один блок данных — это вторая
копия листа, а не избыточность.

---

## 15. 18.09, ночь: умолчания — `-r 0` взято, `--header` отвергнут, и почему

Состояние на момент записи: `master` = `9aaa06c` (тег `v1.4`), в рабочем дереве
**не закоммичено** 17 строк — `src/main.c` и `test_cli.py`. Ветка
`quality-map-and-neighbour-resync` слита и удалена.

### 15.1 `-r 0` по умолчанию — сделано

`pb_redundancy=0` в `src/main.c:91`. Лист A4 при `-d 150 --image-dpi 600`
держит 1422 блока против 1180. Все проверки проходят.

Один тест пришлось привязать к `-r 5` явно: он меряет границу ёмкости
(«44100 байт ровно заполняют первый лист, а 16 байт выравнивания требуют
второго»), и такую границу нельзя оставлять на усмотрение умолчания.

### 15.2 `--header` по умолчанию — **отвергнут, и это находка**

Разреженный лист (4 КБ на A4, сетка в три ряда) с заголовком не читается, и
зависит это от геометрии бумаги:

| бумага | с заголовком | с `-n` |
|---|---|---|
| Letter | **No image** | 46/46 |
| A5 | **No grid** | 46/46 |
| A4, повёрнут на 180° | **No image** | 46/46 |
| A4 вертикально, A3, A6, Legal, Tabloid | 46/46 | 46/46 |

**Механизм.** `Getgridposition` ищет границы растра по профилю локального
контраста (`cmax-cmin` по пяти пикселям в точке) и берёт порог **50% от
максимума**. Края глифов контрастнее точек сетки, поэтому на разреженном листе
максимум задаёт текст, ряды сетки до половины не добирают, и границы
**схлопываются на текстовую полосу**. Дальше окно 1024×1024 садится в текст,
`cmax-cmin<1`, и лист отвергается целиком. Почему одна бумага проходит, а другая
нет, — вопрос того, куда попадает окно, то есть чистой геометрии.

Слабость не новая: `--header` мог это сделать всегда. Но как необязательный флаг
на плотном листе она почти не кусалась, а как умолчание кусается сразу.
Поэтому умолчание оставлено выключенным.

### 15.3 Побочно: `-n` и `--no-header` не работали никогда

`{"no-header",0,0,'n'}` стоит в таблице опций с самого начала порта, а
`case 'n':` был **пустым `break`**. То есть опция годами принималась и молча
игнорировалась. Теперь выключает заголовок — это единственное, что оставлено в
дереве из этой части работы, и проверяется функционально: Letter + `--header`
даёт «No image», Letter + `--header -n` даёт 46/46.

### 15.4 Косметика «SHA-256 под сеткой» — скорее всего это не косметика

Предложение перенести строку с дайджестом из нижнего поля прямо под блок точек
связано с §15.2 сильнее, чем кажется.

Сейчас текст стоит в отдельной полосе, отделённой от сетки белым зазором, и
границы растра **прыгают** на него целиком. Строка вплотную под сеткой окажется
внутри растра: границы не прыгнут, а расширятся на высоту строки — и окно,
центрированное на таких границах, почти наверняка останется на сетке. То есть
перенос может оказаться **функциональным исправлением**, а не косметикой. Но это
гипотеза, наугад ставить нельзя.

### 15.5 Порядок работ, когда вернёмся

1. **Порог в `Getgridposition`**: устойчивая оценка вместо 50% от максимума.
   После правки «привязка по всему листу» (§11) слишком широкие границы почти
   безвредны — профиль суммируется по всему растру и текст в нём разбавляется, —
   а схлопнутые смертельны. Это правка **первого шага каждого декодирования**,
   поэтому мерить по всему набору: 69 синтетических листов плюс реальные сканы.
2. Перенести строку SHA-256 под сетку — уже безопасно.
3. Только после этого включать `--header` по умолчанию.

Пока: печатать с `--header` на плотном листе (все реальные листы здесь плотные),
а на разреженном выключать через `-n`.

### 15.6 Как мерить, чтобы не обмануться

Три ловушки, каждая стоила времени именно в этой сессии — и все три давали
**самосогласованную, но неверную картину**:

- **Скрипт правки упал на середине и не записал ничего.** Правки применялись
  батчем с `assert` на каждый шаблон, запись — в конце. Один непопавший шаблон —
  и не применилось **ничего**, при том что справка (правленная отдельным
  прогоном) уже говорила новое. Полчаса ушло на измерение умолчаний, которых в
  бинарнике не было. Проверять факт правки в файле, а не в своём намерении.
- **Устаревший файл в измерении.** Цикл `--encode -o c.bmp` без `rm` перед ним:
  когда кодирование падало, декодировался **предыдущий** `c.bmp`, и таблица
  выглядела осмысленной. Удалять выход перед каждым прогоном и проверять код
  возврата.
- **Переиспользованный каталог тестов.** Диагностика шла по `page.bmp` из
  каталога, где лежал файл от предыдущего, ещё сломанного прогона. По размеру
  выяснилось, что это вообще Letter, а не A4, то есть падение было совсем в
  другом месте набора. Каталог под каждый прогон — чистый.

---

## 16. 19.09: перенос из копии проекта — взят минимальный набор

В копии `paperback-cli-master — копия` на `master` лежало семь коммитов поверх
`9aaa06c` и ветка `experiment/hash-barcode-markers`. Ветка не берётся: QR ест
ёмкость и не ложится в компоновку; область под сеткой узкая и длинная, так что
при возврате к машинной подписи смотреть надо на 1D, а не на квадрат.

Взято четыре вещи. Нумерация разделов ниже — по журналу копии.

### 16.1 Печать заголовка: серый текст и подвал под сеткой (копия, §16)

`src/Printer.c`: `Drawheadline` рисует уровнем 128 вместо 0, полоса подвала
начинается с `gridtop+height`, а не от нижнего поля листа. Обе правки
возвращают поведение оригинала 1.20, потерянное при портировании.

Механизм — из §15.2: порог поиска растра берётся как половина максимума
профиля локального контраста. Края чёрных глифов контрастнее точек, поэтому
на разреженном листе максимум задаёт текст, а границы схлопываются на текстовую
полосу. Серый текст убирает превосходство глифов, подвал вплотную к сетке
убирает пустую полосу, на которой центрировалось окно поиска.

Измерение воспроизведено здесь заново, не перенесено из копии: 4096 байт
(`bytes(range(256))*16`), `--dpi 100 --header`, семь форматов × 0°/180°,
успех — код 0 и побайтовое совпадение.

| сборка | восстановлений |
|---|---|
| текущий `src/Printer.c` до правки | 9/14 (падали A4 180°, Letter 0°, A5 0° и 180°, Legal 180°) |
| после правки | 14/14 |

Содержание строк не менялось: сверху имя, время, размер и номер страницы,
снизу SHA-256 и рекомендация по сканированию. Умолчание `--header` остаётся
выключенным. Старые отпечатки с чёрным текстом это не чинит.

### 16.2 Проверка структуры адреса до фиксации ориентации (копия, §21–22)

`src/Decoder.c`, `Recognizebits`: после RS и CRC, но **до** присвоения
`pdata->orientation` и `lastgood` проверяется, что адрес вообще пригоден для
сборщика — группа 0..`NGROUPMAX`, смещение меньше `MAXSIZE`, адрес кратен
размеру группы × `NDATA`. `SUPERBLOCK` пропускается как есть, группа 1
оставлена для старых страниц.

Основание — не гипотеза: на реальных сканах `01.bmp` принимал единственный блок
со стираниями с адресом группы 12, `ScanImage50.bmp` — два, группы 15 и 12, при
`NGROUPMAX=10`. Стирания тратят всю избыточность RS, после чего от подделки
защищает только 16-битный CRC; такой ложный приём задавал ориентацию всей
страницы и считался прочитанным блоком. Прежнее утверждение §12.3 «адрес всегда
правдоподобен» этим опровергнуто.

### 16.3 Не повторять тот же проход без прочитанных блоков (копия, §23–24)

`Decodenextblock` начинает второй проход только при `orientation>=0`. Пока
ориентация неизвестна, ни один результат не принят: нет ни новой ориентации, ни
соседей для `Predictorigin`, ни изменения `lastgood`, — второй просмотр той же
сетки даёт тех же кандидатов. В копии это дало `ScanImage50.bmp` 111,79 → 56,27 с
при прежнем отказе; здесь время не перемерялось, сканов в дереве сейчас нет.

### 16.4 README

Строка про `--redundancy` говорила «default 5» при `pb_redundancy=0` в коде.
Приведена в соответствие, заодно описаны серый текст и положение подвала.

### 16.5 Защита `.map` эксклюзивным созданием (из копии, §17 — только первая часть)

Дефект копии подтверждён здесь на сборке до правки: в каталоге с существующим
`label.dat.map` восстановление без `-o` **затирало карту и завершалось кодом 0**.
Карта открывалась обычным `fopen(mapname,"w")`, тогда как основной файл был
защищён проверкой «открыть на чтение — существует — отказать».

`Saverestoredfile` теперь создаёт оба пути через `Createfile`: `O_WRONLY|O_CREAT`
плюс `O_EXCL`, если имя взято с листа, и `O_TRUNC`, если путь назван через `-o`.
Права 0600 задаются при создании, поэтому пляска `umask`/`chmod` вокруг каждого
открытия ушла; `chmod` после записи оставлен — существующий файл при `-o` иначе
сохранит прежние права. Прежняя проверка «открыть на чтение» удалена: между ней
и открытием на запись всё равно есть окно.

Побочно закрыт и второй сценарий из ревью копии: восстановление файла с именем
`foo.map`, совпадающим с картой другого восстановленного файла, теперь получает
отказ, а не молча затирает чужую карту.

Если карта занята, уже созданный в этом прогоне основной файл удаляется. Он
создан эксклюзивно, то есть до прогона его не было — терять нечего, зато совет
«pass -o» остаётся выполнимым, а не упирается в файл, оставленный этой же
попыткой.

**Не взята** остальная часть §17 копии: временный файл рядом с целью, резервная
копия карты и откат при неудачной замене (~70 строк). Это не атомарность —
две операции переименования не являются транзакцией, автор правки это сам
оговаривает, — а обработка обычных ошибок ценой заметного усложнения. При `-o`
поведение осталось прежним: путь назван вызывающим, он перезаписывается.

### 16.6 Что осталось в копии и почему не взято

- **Атомарная публикация восстановления** (`Publishrestore`, ~70 строк сверх
  того, что взято) — см. §16.5: сам дефект закрыт, обвязка вокруг него нет.
- **Диагностика происхождения и конфликтов блоков** (~80 строк, байт признаков
  на адрес). Полезна — единственное, что показывает наружу правило «первый
  принятый блок окончателен». Отложена как средний диффф, не как ошибка.
- **Повторный поиск сетки другими окнами** (~50 строк + `grid_check.py`).
  Даёт 179/444 блоков на листе с затёртой полосой вместо полного отказа.
  Частично перекрыт §16.1 на header-кейсе; зависит от признаков из предыдущего
  пункта. Отложено.
- **Перебор альтернативных блоков под `--expect`** (~90 строк, 64 блока в
  памяти, перебор до 4096 сочетаний). Срабатывает только при одновременном
  совпадении трёх условий и проверен лишь на синтетической коллизии CRC16.
  Отклонено до случая, когда это действительно понадобится.
- **Предэтап поиска ориентации** — отклонён самим автором по измерениям.
- **Правка `M_BEST`** (копирование неинициализированного `bestresult` при
  `bestanswer==17`). Баг настоящий, но `M_BEST` в CLI нигде не выставляется.
  Чинить мёртвую ветку смысла нет; если она не нужна — её стоит удалить.
- Из наблюдений копии, ценных без кода: отключение резкости **ухудшает**
  55.bmp (745 → 720 блоков), то есть эту гипотезу можно закрыть;
  `001.bmp`–`007.bmp` — сгенерированные изображения для печати, а не сканы.

### 16.7 Проверка

`make check` (`clean` + `-O2 -std=gnu11 -Wall -Wextra -Werror`, полный
`test_cli.py`, `test_core.exe`) проходит. Добавлено:

- `test_cli.py`: семь форматов × 0°/180° с `--header`; положение серой подписи
  относительно фактического низа сетки; двухстраничные файлы 56000 байт при
  `-r 0` и `-r 5`, в обеих ориентациях. Порог `inked` изменён с `<128` на
  `<255`, иначе серый текст перестаёт считаться напечатанным.
- `test_core.c`: `Recognizebits` вызывается напрямую с настоящими RS-кодовыми
  словами — обычные адреса, чётность групп 1/5/10, `SUPERBLOCK`, невыравненные
  адреса, группа 11, адрес за `MAXSIZE` и три ложных адреса из реальных сканов;
  обычный режим и `M_BEST`, со стираниями и без. Отклонённый адрес обязан
  оставить `orientation=-1`. Второй тест проверяет конец прохода при
  известной/неизвестной ориентации.
- `test_cli.py`: восстановление без `-o` в каталоге с существующей
  `label.dat.map` завершается кодом 1, карта сохраняет прежние байты, и
  основной файл, созданный этой попыткой, не остаётся на диске. Проверено, что
  на сборке до правки этот сценарий даёт код 0 и затёртую карту.
- Ради доступа к статическим функциям `test_core.c` включает `src/Decoder.c`
  напрямую, как уже включал `main.c`; Makefile исключает его из линковки,
  в `paperbak.h` добавлена защита от повторного включения.

Ветка POSIX в `Createfile` (`open`/`fdopen`/`close`, `unistd.h`) на этой
Windows-машине не собиралась: заголовки mingw отказываются компилироваться без
`_WIN32`, поэтому проверка ограничена чтением. Заполнение диска не имитировалось.

Ограничения: реальные сканы (`75.bmp`, `55.bmp`, `01.bmp`, `ScanImage50.bmp`)
в дереве сейчас отсутствуют, поэтому заявления копии «910/910 и 745/910 без
изменений» здесь **не перепроверены**. Проверять при первом же случае, когда
сканы вернутся. Правка §16.2 защищает выбор ориентации, но не устраняет ложные
приёмы со структурно допустимым адресом; итоговой проверкой остаётся SHA-256.

---

## 17. 19.09: сканы вернулись на место, и повторный поиск сетки

### 17.1 Сканы

`experiments/` игнорируется целиком (`*` с исключениями), поэтому в §16.7 было
записано, что сканов в дереве нет. Это неверно: часть лежала на месте, а
остального не хватало. Из копии перенесены 003–007, 55, 70, 75, 85, 100 и
ScanImage50; пять уже лежавших сверены по SHA-256 и идентичны. `01.bmp` и
`ScanImage50.bmp` совпали с идентификаторами, записанными в §21 копии, — это
те самые файлы, а не одноимённые.

Долг §16.7 закрыт на текущем `master` (то есть уже с правками §16.2 и §16.3):

| скан | результат | выход |
|---|---|---|
| 75.bmp | 910/910, 3 с | `2c7e91ee…` |
| 55.bmp | 745/910, 60 XOR, 13 с | `2b106db2…` |

Побайтно совпадает с тем, что записано в копии. `01.bmp` и `ScanImage50.bmp`
по-прежнему отвергаются: 107 и 58 с, метка не читается.

### 17.2 Повторный поиск сетки (из копии, 8514ba2)

`Retrygrid` перезапускает локализацию с границами растра по всему изображению
и окном в одной из девяти позиций (центр, края, углы). Быстрый путь не тронут:
повтор бывает только после отказа — `No image`, `No grid` либо завершение
прохода, в котором не прочитан ни один блок и нет метки страницы. Между
попытками освобождаются рабочие буферы, само изображение сохраняется.

Вместо признаков происхождения блоков (~80 строк, §16.6) взято одно поле
`usederasures` в `t_procdata`: `Recognizebits` помечает, понадобились ли
принятому кандидату стирания, и метка страницы, принятая только так, не
подтверждает резервное окно. Четыре строки вместо восьмидесяти; в основной
буфер сборщика ничего не добавляется.

`experiments/grid_check.py` из копии включён в `make test/check/sanitize`.
Он сам строит фикстуры: воссоздаёт старую компоновку заголовка (чёрный текст,
подвал у нижнего поля) из текущего кодировщика, повреждает плотный лист,
делает пустой лист, только текст и воспроизводимый шум. Результаты совпали с
записанными в копии:

| случай | результат |
|---|---|
| 14 старых заголовков (7 форматов × 0°/180°) | все побайтно точно |
| затёртая вертикальная полоса | 179/444 вместо отсутствия выхода |
| затёртый центр / горизонтальная полоса | 302/444 и 206/444, как было |
| плотный лист | читается без единого повтора |
| пустой лист, только текст, шум | отвергнуты |

### 17.3 Проверенная гипотеза: дорогая ветка нужна

Было предположение, что выигрыш дают дешёвые повторы локализации, а повтор
после полного прохода (ngood==0) можно выбросить и тем снять расходы. Собран
вариант без него: `vertical` немедленно упал со 179/444 до отсутствия выхода.
То есть спасает этот лист именно дорогая ветка. Гипотеза отклонена, ветка
оставлена.

### 17.4 Цена: отказ стал заметно дольше

Ограничитель «есть прочитанный блок — повторов не делать» после §16.2 работает
иначе, чем в копии: ложные приёмы со стираниями больше не считаются за
прочитанный блок, поэтому нечитаемые сканы теперь честно проходят все окна.

| скан | до | после | попыток |
|---|---|---|---|
| ScanImage50.bmp | 58 с | 184 с | 3 |
| 01.bmp | 107 с | 537 с | 9 |

Результат обоих прежний — отказ без выходного файла. Читаемые сканы не
затронуты вовсе: 75.bmp 3 с и 55.bmp 13 с, повторов ноль, выходы побайтно те
же. То есть плата берётся только с листа, который всё равно не прочитан, но
берётся полностью: девять фиксированных окон. В коде это помечено
`ponytail:`-комментарием — заменой на бюджет по времени, если появится скан,
который требуется именно прочитать, а не быстрее отвергнуть.

Одиночные замеры на одной машине, не статистика.

---

## 18. 19.09: где промахивается порог поиска растра, и чем он заменён

### 18.1 Чем мерили

`experiments/grid_probe.py`: отдельная сборка декодера из копии `Decoder.c`,
в которую добавлен дамп профиля локального контраста и найденных границ.
Штатные исходники проба не меняет. Эталон для синтетических листов — рамка
чёрных точек (точки 0, серый текст 128, поэтому текст в рамку не попадает);
для листов со старым чёрным заголовком берётся рамка исходного серого листа,
так как переносится только текст. Координаты декодерные: строка 0 — низ листа.

Критерий промаха взят по механизму отказа: доля растра в окне 1024×1024,
которое `Getgridintensity` ставит в середину найденных границ. Ноль — смерть:
внутри окна нет контраста, `cmax-cmin<1`, лист отвергается целиком.

### 18.2 Где промахивается

Проба воспроизводит исторический отказ ровно: окно уходит с растра на пяти
листах из четырнадцати — A4 180°, Letter 0°, A5 0°, A5 180°, Legal 180°. Это
те же пять, что в §16.1. Ошибка границы на них — тысячи пикселей
(`ymax +2865`, `ymin -2647`, `ymin -1725`, `ymax +1726`, `ymax +3554`).
С серым текстом промахов нет ни одного: ошибка не больше 66 px.

### 18.3 Механизм оказался не тем, что записано в §15.2

Медиана профиля по строкам растра против пика по строкам вне его, в долях
максимума:

| лист | растр | текст |
|---|---|---|
| A5 180° | 0.70 | 0.88 |
| Letter | 0.66 | 0.76 |
| A5 | 0.45 | 0.54 |

**Порог проходят обе области.** Утверждение §15.2 «ряды сетки до половины не
добирают» неверно: добирают. Граница берётся как первый и последний отсчёт
выше половины максимума, поэтому интервал растягивается от текстовой полосы
до растра, а его середина попадает в белый зазор между ними.

Отсюда прямое следствие: **никакой глобальный порог этого не чинит.** На
A5 180° текст (0.88) контрастнее медианы растра (0.70); порог, отсекающий
текст, отрежет и половину растра. Это объясняет, почему в копии эксперимент
«среднее вместо максимума» ухудшил результат с 9/14 до 2/14 — крутили ручку,
которая к этому отказу отношения не имеет.

Разделяет области не уровень, а форма: растр длинный, строка текста короткая.

### 18.4 Первая попытка и алиасинг

Правило «самый длинный отрезок выше порога» на профилях дало 0 промахов из 25
против 5 у действующего. Но сборка показала другое: A4 и A6 (в том числе с
серым заголовком) стали требовать повторов там, где раньше читались сразу.

Причина в профиле: он **алиасится**. Отсчётов не более 256 на ось, то есть
шаг 14 px на A4 против ячейки в 3 px, и соседние отсчёты попадают то на ряд
точек, то между рядами. Растр выглядит пунктиром:

| лист | отрезков выше порога внутри растра |
|---|---|
| A4 | 8, каждый длиной 0 |
| A6 | 30, каждый длиной 0 |
| Letter | 182 и 78 — здесь повезло с фазой |

На реальных сканах то же: строгий «длинный отрезок» срезал бы у ScanImage50
3400 px растра (`336..6496` → `3780..6496`), у 01.bmp — 700 px.

Поэтому отрезок должен терпеть разрывы. Сколько именно — измерено, а не
выбрано: разрывы внутри растра доходят до **6** отсчётов, ближайшая текстовая
полоса отстоит минимум на **112**. `GRIDGAP=16` лежит между ними с запасом
почти втрое от разрывов и всемеро от текста.

### 18.5 Результат

`Widestrun` в `src/Decoder.c`: самый широкий участок профиля выше половины
максимума, разрывы до `GRIDGAP` отсчётов не разрывают участок.

| набор | было | стало |
|---|---|---|
| 7 серых листов | 0 повторов, точно | 0 повторов, точно |
| 14 старых заголовков | 16 повторов на 6 листах, точно | **0 повторов**, точно |
| затёртые центр / полоса / горизонталь | 302 / 179 / 206 | те же, повторов 0 вместо 4 |
| 75.bmp | 910/910 | 910/910, байт в байт |
| 55.bmp | 745/910 | 745/910, байт в байт |
| пустой лист, текст, шум | отвергнуты | отвергнуты |
| ScanImage50.bmp | отказ, 184 с, 3 повтора | отказ, 154 с, 3 повтора |
| 01.bmp | отказ, 537 с, 9 повторов | отказ, 525 с, 9 повторов |

Ни одного изменения результата: меняется только то, что локализация попадает
с первой попытки. `make check` (`test_cli.py`, `grid_check.py --check`,
`test_core`) проходит.

Отдельно стоит `vertical` — лист с затёртой вертикальной полосой, ради
которого в §17.3 была оставлена дорогая ветка повтора: он больше в ней не
нуждается. Ветку не убираем — она остаётся страховкой и всё ещё единственное,
что спасает этот лист, если локализация всё же промахнётся, — но перебор окон
из `85ef4f3` вернулся к роли резерва, а не основного механизма для листов с
заголовком.

### 18.6 Чего это не делает

Не помогает `01.bmp` и `ScanImage50.bmp`: их отказ не в локализации. Границы
там и раньше были правдоподобны, а §21 копии показывает, что читаются они
вообще никак — ни одного блока обычным RS. Следующий предмет разбора остаётся
прежним, из §25: сравнение считанных точек с исходными битами до Рида-Соломона.

Замеры синтетики — детерминированные, времена сканов — одиночные, на одной
машине, при параллельно идущих задачах.

---

## 19. 19.09: набор пар печать/скан вместо реконструкции соответствия

### 19.1 Почему не §25 шаг 1

План §25 начинался с сопоставления `01.bmp` и `ScanImage50.bmp` с исходниками
`001.bmp`–`007.bmp` по рисунку блоков. От этого отказываемся, и вот почему.

Даже при совпадении рисунка эталон держится на цепочке допущений: тот ли лист,
те ли параметры кодирования, не было ли масштабирования. Соответствие
пользователь не помнит, про `--image-dpi 600` известно, что так печаталась
лишь часть листов. Ошибки, посчитанные относительно неверного эталона, выйдут
самосогласованными — ровно тот тип ловушки, что описан в §15.6.

И решающее: на `01.bmp` не принимается ни одного блока, ориентация не
определяется (§21). Значит нет геометрии, а без неё неизвестно, какому пикселю
скана соответствует какая точка источника. Сравнивать точки не с чем — этот
файл для дот-левел анализа непригоден в принципе, чем бы ни была его причина.

Оба скана остаются в дереве как регрессия «читается / не читается»: если
будущая правка их возьмёт, это победа, и эталон для такой проверки не нужен.

### 19.2 Что вместо этого

`experiments/print_set.py` строит шесть листов с одним и тем же входом
(20000 байт, зерно фиксировано, поэтому вход восстановим) и пишет
`MANIFEST.md`: команда, SHA-256 входа и каждого листа, геометрия, и **имя
файла, под которым надо сохранить скан**. Последнее и есть то, чего лишён
старый набор.

| лист | вопрос |
|---|---|
| `-d 100` | опора: эталонная форма точки на этой бумаге |
| `-d 150` | умолчание; этот лист сканируется дважды, 600 и 300 dpi |
| `-d 150 --header` | серый заголовок и подвал под сеткой на бумаге; §16.1 мерил только цифру |
| `-d 175 -s 60` | между рабочей плотностью и «невозможной», точка меньше |
| `-d 200` | проверка категоричного вывода §14 |
| `-d 200 -s 50` | та же плотность, минимальная точка: если дело только в растекании, этот читается, а `-d 200 -s 70` нет |

Все шесть **декодируются из сгенерированного изображения побайтно** (223/223),
и это записано в манифест. Проверка встроена в сборку набора: лист, который
не читается ещё до бумаги, не должен уходить в печать. Благодаря ей всё, что
потеряется на скане, потеряно между принтером и стеклом сканера, а не в
кодировании.

Плотность одна для всех листов по смыслу: содержимое одинаково, поэтому блок,
читающийся на одном листе и не читающийся на другом, отличается печатью, а не
данными.

### 19.3 Что дальше

Печать при 100% масштабе, скан 600 dpi в оттенках серого без автообрезки,
выравнивания и повышения резкости; `02` дополнительно на 300 dpi, чтобы
отделить предел принтера от предела сканера. Сканы кладутся в
`experiments/print-set/scans/` под именами из манифеста.

Когда они появятся: при успешной локализации декодер знает начало, шаг и угол
каждого блока, поэтому проба может взять его геометрию и сличить считанные
точки с исходными битами, не изобретая привязку. Это и есть измерение,
которого хотел §25, — но на паре, где эталон известен точно.

Лист на грани читаемости ценнее нечитаемого: сетка находится, геометрия есть,
и видно каждую ошибочную точку. Поэтому в наборе есть и заведомо трудные `05`
и `06`.


---

## 20. 19.09: сканы с железа — бумага держит `-d 100`, а порог декодера её не дочитывает

### 20.1 Семь сканов

Набор §19 вернулся напечатанным и отсканированным целиком, под именами из
манифеста. Вход у всех листов один и тот же, 20000 байт,
`337633b5…4c9bc5`, и все шесть листов до печати декодировались побайтно.
Что получилось на бумаге (декодер до правок §20.4):

| скан | блоков из 223 | ширина точки | что это значит |
|---|---|---|---|
| `01-d100-s70-600dpi` | **223/223**, SHA сошлась | 73% против 70% печатных | бумага держит `-d 100` |
| `02-d150-s70-600dpi` | 4/223 | 90% против 75% | умолчание на этом принтере не читается |
| `03-d150-s70-header-600dpi` | 6/223 | 91% против 75% | заголовок не мешает: растр найден, как и без него |
| `02-d150-s70-300dpi` | растр найден, метка нечитаема | — | 2 пикселя на клетку мало |
| `04-d175-s60-600dpi` | растра нет | — | |
| `05-d200-s70-600dpi` | растра нет | — | §14 подтверждён на бумаге |
| `06-d200-s50-600dpi` | растра нет | — | меньшая точка не спасает |

Сканировать надо вчетверо плотнее кода: 600 dpi против `-d 150` — это 4
пикселя на клетку, и лист читается хотя бы частично; 300 dpi — 2 пикселя, и
метка страницы уже не собирается. Предел сканера здесь наступает раньше предела
принтера, ради этого лист `02` и сканировался дважды.

### 20.2 «Печать в 100%» — это 97%

Растр на листе `01` занимает 4441x2341 px, на скане того же листа — 4309x2267.
По девяти привязкам аффинное преобразование выходит 0.9702 по x и 0.9674 по y,
на `02` — 0.9707 и 0.9689, смещение +70…+75 px по x и +127…+150 px по y.
То есть принтер сжал лист до 97%, и это не разовая случайность: коэффициент
одинаков на всех шести листах.

Декодер это переживает, потому что шаг сетки он измеряет, а не берёт из
номинала (у скана в карте на один блок по каждой оси больше, чем у исходного
изображения — то же самое другими словами). Но локальная привязка всё равно
нужна: остаточный сдвиг гуляет на ±4 px по листу, а это больше клетки `-d 150`.

### 20.3 Сколько точек уцелело на самом деле

`experiments/dot_diff.py` сличает лист с его сканом без сетки, без
ориентации и без Рида-Соломона: рамка растра даёт первый масштаб, девять
пятен уточняют его до аффинного, скан разворачивается в геометрию листа,
плитки по 32 клетки снимают остаточный сдвиг, и дальше каждая клетка
сравнивается с чернилами, которые в неё печатались. Это и есть измерение,
которого хотел §25, — на паре, где эталон известен точно.

Числа ниже — уже с исправленной привязкой плиток, см. 21.1; первая версия
этой таблицы завышала качество бумаги.

| лист | ошибочных точек, лучший порог | на блок (1024 точки) | середина между чернилами и бумагой |
|---|---|---|---|
| `01-d100-s70` | **0.479%** | 4.9 | 1.79% |
| `02-d150-s70` | 3.54% | 36 | 6.04% |
| `03-d150-s70-header` | 3.72% | 38 | 5.98% |
| `04-d175-s60` | 8.94% | 92 | — |
| `05-d200-s70` | 8.68% | 89 | 10.9% |
| `06-d200-s50` | 8.92% | 91 | — |

Рид-Соломон правит 16 байт из 128 в блоке. 4.9 ошибочной точки на блок — это
запас втрое, 36 — приговор, 90 — там нечего читать, и потому у `-d 200` не
находится даже растр: бумага возвращает серую кашу, а не точки. `-d 150` не
спасти ничем на уровне решения «точка или бумага»: даже при идеальном пороге
это 36 битых байт против шестнадцати возможных. Что его спасает, выяснилось
позже и не в коде — §21.

Листы `04`, `05` и `06` — одно и то же изображение (см. 20.6), напечатанное и
снятое трижды. Разброс 8.68/8.92/8.94% и есть повторяемость всей цепочки
печать-бумага-стекло: 0.26 процентного пункта. Значит разница в полпункта
между листами — настоящая.

### 20.4 Порог стоит не посередине, и это стоило 13 точек на блок

Лучший порог на `01` лежит не между чернилами и бумагой, а на 80–85% пути к
бумаге (t=205…212 при чернилах 86 и бумаге 235). Причина простая: пустая
клетка возвращается плотной кучкой у самой бумаги, а напечатанная точка —
чем угодно от чёрного до почти бумаги. Порог посередине режет светлый хвост
точек и объявляет их бумагой.

Цифры по `01`, ошибочных точек на блок: 12.9 при 0.50, 6.2 при 0.60, 2.6 при
0.70, 1.0 при 0.80, 0.9 при 0.85, 3.7 при 0.90 и 42.8 при 0.95. Оптимум
широкий, обрыв за ним крутой.

`Recognizebits` перебирал три множителя перекрытия против трёх порогов, и
дальше `±(cmax-cmin)/16` не доставал — это 6% диапазона там, где нужно около
20%. Лестница порогов расширена до пяти шагов, `0, -1, +1, +2, +3` шестнадцатых
диапазона, пятнадцать комбинаций вместо девяти; `lastgood` по-прежнему ставит
удачную первой, так что на блок приходится примерно одна попытка.

Что дала правка на тех же сканах:

| скан | было | стало |
|---|---|---|
| `01-d100-s70-600dpi` | 223/223, почти все блоки 10–16 правок | 223/223, правок 3–9 у большинства блоков |
| `02-d150-s70-600dpi` | 4/223 | **40/223** |
| `03-d150-s70-header-600dpi` | 6/223 | **33/223** |

Сгенерированные изображения всех шести листов по-прежнему читаются 223/223 с
нулём правок, `test_cli.py` и `test_core.exe` проходят. `-d 150` от этого
читаемым не стал и стать не мог — см. 20.3.

### 20.5 Карта качества показывала не то, что обещала

Легенда карты говорит «сколько байт правил ECC у этого блока», но декодер
останавливается на первой комбинации порога, прошедшей CRC, а не на лучшей.
Поиск лучшей в коде есть — `M_BEST`, — и включается он флагом
`pb_bestquality`, который не выставлялся никогда и ниоткуда: объявлен и
забыт. Теперь `--quality-map` его включает, и цифры в карте стали настоящими
(лист `01`: 3…16 правок вместо «почти всё 10+»; на этот лист это стоило
0.6 секунды из 4).

### 20.6 Три листа набора оказались одним

`04-d175-s60`, `05-d200-s70` и `06-d200-s50` побайтно одинаковы, SHA-256 у
них один: `931b6f…5c7dc5`. При `--image-dpi 600` клетка обязана быть целым
числом пикселей, поэтому `-d 175` округляется до 600/3 = 200 dpi, а `-s 60` и
`-s 50` от трёхпиксельной клетки дают ту же двухпиксельную точку, что и
`-s 70`. Манифест это честно напечатал («actual dot density 200.00 DPI» у
листа, заказанного как 175), но `print_set.py` не сравнил SHA между листами, а
я не посмотрел. Вопрос «дело только в растекании?» из §19 набором не проверен:
три листа ушли на одну конфигурацию.

Проверка добавлена в `print_set.py`: одинаковый SHA у двух листов теперь
роняет сборку набора. Она же вскрыла второе: лист несёт время изменения
входного файла (`Printer.c`, `print->modified`), а `print_set.py` перезаписывал
`input.bin` при каждом запуске, поэтому дважды собранный набор — это два разных
набора битов при одних и тех же байтах. Время входа зафиксировано константой,
и листы стали воспроизводимы побайтно (проверено: два прогона подряд дают один
SHA). Но напечатанные листы собирались до этой правки, и их точные байты
восстановить нечем: в суперблоке лежала тогдашняя метка времени с точностью
FILETIME. Данные в блоках от этого не зависят, различие — суперблок, один блок
из 223, так что замеры 20.3 это трогает в сотых долях процента; знать об этом
всё равно надо. Старые `04-d175-s60.bmp` и `06-d200-s50.bmp` остались лежать
рядом с набором как пара к своим сканам, в манифесте их больше нет.

Плотности при `--image-dpi 600` ступенчатые: 600, 300, 200, 150, 120, 100, 85.7…
Между 150 и 200 нет ничего, и `-s` на клетке из трёх пикселей не значит
ничего. Между `-d 100` (работает) и `-d 150` (не работает) лежит `-d 120`
(клетка 5 px) — вот его и надо печатать следующим, а `-s` проверять на клетке
из шести пикселей, где он ещё что-то меняет. Набор пересобран под это:
`04-d120-s70` вместо `-d 175`, и `06-d100-s50` вместо второго `-d 200` —
базовая плотность с минимальной точкой, то есть тот же вопрос про растекание,
но на листе, который читается, и потому каждую потерянную точку можно
посчитать.

### 20.7 Что дальше

Порог был не последней потерей, но и не такой большой, как показалось сначала:
при идеальной привязке бумага на `-d 100` отдаёт 4.9 битой точки на блок, а
декодер правит 3–9 байт, то есть идёт почти по пределу бумаги (первая версия
этого абзаца утверждала разрыв в порядок величины — это была ошибка пробы,
21.1). Запас, если он где-то и остался, лежит между `g[shift][j][i]` и `limit`:
локальный порог вместо блочного, взвешивание по расстоянию до порога.

Умолчание `-d 150` на этом железе не читается, `-d 100` читается с запасом.
Менять умолчание по одному принтеру не стоит, но выбор плотности — не вопрос
вкуса, и README про это молчит.

Границы измерения: один принтер, один сканер, по одному скану на лист (кроме
`04`–`06`, которые дали повторяемость 0.1 п.п.). `dot_diff.py` сдвигает
плитки на целые пиксели и берёт один порог на лист, поэтому его проценты —
оценка сверху для потерь бумаги и снизу для того, что может вытащить декодер.

---

## 21. 19.09, позже: «Повысить чёткость печати» — и умолчание читается побайтно

Второй прогон того же набора на том же железе, одно изменение: в драйвере
принтера включена «Повысить чёткость печати». Сканы в `print-set/scans2/`,
имена те же. Печатались листы прежнего состава (`04-d175-s60`, `06-d200-s50`),
то есть d200 снова в трёх копиях — зато это снова даёт повторяемость.

### 21.1 Сначала поправка: у пробы была своя привязка

Сравнение двух прогонов немедленно показало, что `dot_diff.py` даёт разные
числа на одном и том же скане при безобидных правках. Причина в нём самом:
сдвиг плитки искался FFT-корреляцией в окне шириной с саму плитку, а растр из
одинаковых точек в таком окне корреллирует почти одинаково хорошо и на клетку
в сторону. Отдельные плитки уезжали на 100–170 px, и каждая такая — тысяча
«потерянных» точек из воздуха.

Что сделано: аффинное по девяти пятнам осталось (оно работает, невязка ~2.5 px),
а сдвиг плитки теперь ищется прямым перебором ±6 px в два прохода — сначала по
разделению чернил и бумаги, потом по числу расхождений при найденном пороге.
Рваные плитки по краю растра отброшены: в плитке шириной в четыре клетки
чернил слишком мало, чтобы выбрать сдвиг, и именно они давали «градиент по
листу», который я чуть не принял за неравномерность печати. Числа §20.3
пересчитаны на месте, вывод §20.7 поправлен.

### 21.2 Что дала чёткость печати

| лист | точек потеряно, чёткость off → on | блоков из 223, off → on |
|---|---|---|
| `01-d100-s70` | 0.479% → 0.432% | 223/223 → 223/223 |
| `02-d150-s70` | 3.54% → **0.742%** | 40/223 → **223/223, SHA сошлась** |
| `03-d150-s70-header` | 3.72% → **0.770%** | 33/223 → **223/223, SHA сошлась** |
| `02` на 300 dpi | — | метка нечитаема → **146/223** |
| `04-d175-s60` | 8.94% → 6.34% | нет растра → нет растра |
| `05-d200-s70` | 8.68% → 6.05% | нет растра → нет растра |
| `06-d200-s50` | 8.92% → 5.99% | нет растра → нет растра |

Умолчание `-d 150` на этом железе читается. Одна галка в драйвере — и лист из
четырёх блоков из 223 становится байт в байт, без избыточности, `-r 0`.
Повторяемость по трём копиям d200 во втором прогоне 0.35 п.п. (5.99/6.05/6.34),
в первом 0.26 — разница 2.6 п.п. на порядок больше шума.

Проба и декодер согласуются: 0.742% — это 7.6 битой точки на блок, меньше
шестнадцати исправимых байт, лист обязан читаться, и он читается. 3.54% — это
36, лист обязан не читаться, и он не читается.

### 21.3 Точки стали темнее, а не уже

Линейка декодера: `-d 100` 73% → 80% ширины клетки, `-d 150` 90% → 96% против
75% напечатанных. Точки стали **шире**, зазор между ними при `-d 150` — 0.16 px
из 4, и лист при этом читается идеально. Значит «зазор между точками», на
котором стоял вывод §14, решает не он.

Решает чернота. Средний уровень чернил: 88 → 55 при `-d 150`, 94 → 76 при
`-d 100`, бумага почти на месте (219 → 205, 233 → 226). И сразу исчезла
асимметрия §20.4: в первом прогоне лучший порог стоял на 80% пути к бумаге, во
втором он совпал с серединой (132 против 130). То есть §20.4 лечил симптом —
бледную точку, которую порог посередине объявляет бумагой, — а принтер лечит
причину. Обе правки по-прежнему нужны: расширенная лестница порогов ничего не
стоит и вытаскивает бледный лист, но вытаскивать его лучше не приходится.

### 21.4 Что это меняет в инструкции

Манифест велел выключить «enhance» вместе с масштабированием и экономией
тонера — и это была ошибка, стоившая первого прогона. Текст в `print_set.py`
переписан: чёткость печати **включить**, а вот резкость в **сканере**
по-прежнему выключить — там она срезает частичные пиксели по краю точки, то
есть ровно то, чем декодер измеряет ширину (и о чём он предупреждает).

### 21.5 `-d 200` мёртв по-прежнему, 300 dpi ожил наполовину

6.0% потерянных точек — это 62 на блок при шестнадцати исправимых. Растр не
находится, и находить там нечего: две печатных точки на три пикселя сливаются в
серое независимо от `-s`.

`02` на 300 dpi собрал 146 блоков из 223 вместо «метка нечитаема». Два пикселя
на клетку — это граница, а не запрет, и на тёмной точке она проходима.
Числа `dot_diff.py` для этой пары не беру: 128 из 272 плиток просят сдвиг
больше ±6 px, проба на смене разрешения неприменима, и её 22% ничего не
измеряют.

### 21.6 Что дальше

Печатать следующий набор (`04-d120-s70`, `06-d100-s50`) — с чёткостью — и
проверить `-d 120`: между 100 и 150 это единственная ступень, а `-d 150` теперь
верхняя рабочая, а не сломанная.

Границы: один принтер, один сканер, по одному скану на лист кроме трёх копий
d200. Проценты `dot_diff.py` — это то, что уцелело при идеальной привязке
плитками по 32 клетки и одном пороге на лист: оценка сверху для бумаги и снизу
для потерь декодера.

## 22. 19.09, вечер: третий прогон — `-d 120` берётся даром, `-s 100` опровергнут, и решает не чернота, а разделение

Сканы в `print-set/scans3/`. Одно изменение против §21: набор листов новый
(`03-d150-s100`, `04-d120-s70`, `05-d150-s50`, `06-d100-s50` вместо мёртвых
d175/d200), чёткость печати включена, как велит манифест. Имена без суффикса
`-600dpi`, повторного скана `02` на 300 dpi нет — второй вопрос §21.5 остался
без ответа.

### 22.1 Сначала поправка: проба мерила точку не той апертурой

`dot_diff.py` определяет окно выборки по профилю чернил самого листа
(`dotspan`), и на двух из шести листов оно оказалось меньше напечатанной точки:

- `03-d150-s100` — зазора нет вовсе, профиль плоский (`[.464 .464 .464 .464]`),
  и «самый широкий прогон» выбирался шумом: **3x1** вместо 4x4.
- `04-d120-s70` — точка лежит через край клетки (`[.38 .38 .02 .38 .38]`),
  прогон разорван краем массива, и берётся длинная половина: **2x2** вместо 4x4.

Обе — один корень: `widestrun` линеен и предполагает, что зазор существует.
Профиль периодичен, поэтому теперь он ищется по двум периодам, а плоский
профиль означает «точка занимает клетку целиком» и объявляется ею явно.
Оба случая добавлены в `--self-test`. Числа ниже — после правки; до неё
`03` давало 0.439%, а `04` — 0.000%, и оба были измерены сквозь щель.

### 22.2 Шесть листов

| лист | клетка | точка | чернила | бумага | разделение | потеряно точек | блоков из 223 |
|---|---|---|---|---|---|---|---|
| `01-d100-s70` | 6 px | 4x4 | 65 | 227 | 162 | 0.000% | **223** |
| `06-d100-s50` | 6 px | 3x3 | 97 | 238 | 140 | 0.005% | **223** |
| `04-d120-s70` | 5 px | 4x4 | 50 | 208 | 158 | 0.003% | **223** |
| `05-d150-s50` | 4 px | 2x2 | 91 | 229 | 138 | 1.992% | 141 |
| `02-d150-s70` | 4 px | 3x3 | 54 | 205 | 151 | 0.345% | **223** |
| `03-d150-s100` | 4 px | 4x4 | 32 | 157 | 125 | 1.994% | 221 |

`02` повторил §21 байт в байт — то же железо даёт тот же результат, прогон
воспроизводим. Масштаб печати на всех шести листах 0.972 — те же 97%.

### 22.3 `-s 100` опровергнут, и вместе с ним обе прежние версии

§14 объяснял отказ закрывающимся зазором, §21.3 возразил: решает чернота,
зазор ни при чём. Лист, где зазора нет совсем, проверяет обе версии сразу —
и опровергает обе.

Чернота §21.3 получена: `-s 100` даёт самые тёмные чернила набора, 32 против
54 у рабочего умолчания. И лист при этом худший из читаемых: 1.994% точек,
221/223. Потому что вместе с чернилами потемнела и бумага между точками —
157 против 205, — и **разделение упало**: 125 против 151.

То есть обе версии мерили одно и то же с разных сторон. Зазор §14 влиял
только через разделение; чернота §21.3 влияет только через разделение. Само
разделение и есть величина, а не её следствие.

### 22.4 Но разделения мало: шаг растра даёт запас

Порядок по разделению почти совпадает с порядком по ошибкам — и ломается на
`06-d100-s50`: разделение 140, ошибок 0.005%, тогда как `05-d150-s50` с
разделением 138 теряет 1.992%. Разница между ними только в шаге: 6 px против 4.

Так что осей две. При `-d 100` читается всё, каким бы ни был размер точки —
0.000% и 0.005% на краях диапазона `-s`. При `-d 150` запаса не осталось
вовсе: работает ровно `-s 70`, а шаг влево (2 px точки) и шаг вправо (4 px)
одинаково дают около 2% и теряют блоки. Умолчание стоит на вершине узкого
горба, а не на плато.

### 22.5 `-d 120` — единственная ступень, и она бесплатная

0.003% точек, 223/223, и порог посередине даёт те же 0.006%: этому листу не
нужна даже расширенная лестница порогов. По качеству он неотличим от `-d 100`,
а ёмкости даёт в 1.44 раза больше. Между «всегда читается» и «читается на
пределе» ступень ровно одна, и она на стороне «всегда».

### 22.6 Отказы `-d 150` асимметричны

`05` и `03` теряют почти поровну точек (1.992% и 1.994%) и расходятся на
80 блоков. Расходятся и по знаку ошибки:

- `05` (точка 2 px): 4796 пропущенных против 753 ложных, лучший порог уехал
  к бумаге (202 при середине 160). Бледную точку принтер не положил, и
  порогом её не вернуть — 141/223.
- `03` (точка 4 px): 3392 ложных против 2163 пропущенных, лучший порог уехал
  к чернилам (68 при середине 94). Чернила растекаются туда, где зазора нет.
  Декодер берёт порог на блок, а не на лист, и вытаскивает 221/223.

Отсюда же и предел самой пробы: один порог на лист — оценка сверху для
бумаги, но по блокам она предсказывает только знак, не число (§21.1 о том же
с другой стороны).

### 22.7 Расширенная лестница порогов окупилась

`06-d100-s50`: 1.254% при пороге посередине против 0.005% при лучшем — в 250
раз. Это ровно бледная точка §20.4 на листе, который иначе был бы краевым.
`05`: 4.187% против 1.992%. Правка e8a3a97 ничего не стоит и держит два листа
из шести.

### 22.8 Что дальше

- Пересканировать `02-d150-s70` на 300 dpi: вопрос §21.5 (146/223) всё ещё
  открыт, лист напечатан.
- `-d 120` стоит посчитать кандидатом в умолчание вместо `-d 150`: та же
  бумага, тот же принтер, запас вместо его отсутствия.
- Проверить `-s 60` и `-s 80` при `-d 150`: горб §22.4 измерен тремя точками,
  его ширина — нет.

Границы прежние: один принтер, один сканер, по одному скану на лист.

### 22.9 Поправка к 22.3: разделение разделению рознь

Декодер уже считает уровни чернил и бумаги (`Getlevels`, 3%-хвосты по сырому
скану) и выбрасывал их — `--quality-map` печатал только ширину точки, а она по
построению меряется на половине собственной глубины и к глубине слепа. Теперь
печатает; строка бесплатная, числа уже были в руках. И они сразу же
опровергают половину §22.3.

| лист | среднее по апертуре (`dot_diff`) | 3%-хвосты по листу (декодер) | блоков |
|---|---|---|---|
| `01-d100-s70` | 162 | 214 | 223 |
| `04-d120-s70` | 158 | 213 | 223 |
| `02-d150-s70` | 151 | 208 | 223 |
| `06-d100-s50` | 140 | 210 | 223 |
| `05-d150-s50` | 138 | 196 | 141 |
| `03-d150-s100` | 125 | 207 | 221 |

Среднее по апертуре ранжирует набор идеально: четыре читаемых листа — четыре
верхних, два отказавших — два нижних. Хвосты не ранжируют вообще: 196..214 на
всём наборе, и `03` со своими 207 сидит посреди читаемых.

Разница в том, что меряется. Хвосты — самые тёмные ядра против самой чистой
бумаги; они держатся, пока на листе есть хоть одна нормальная точка и хоть
один чистый промежуток. Среднее по апертуре включает частичное покрытие и
размытие, то есть ровно то, что теряет декодер.

Отсюда две вещи. Вывод §22.3 («решает разделение») верен только для среднего
по апертуре — формулировку надо читать так. И декодер этой величины не видит:
чтобы её померить, нужен исходный растр, которого у него нет. Считать её по
клеткам, которые он сам объявил чернилами, — круг, но, возможно, рабочий;
это отдельный опыт, а не правка. Напечатанная строка ловит смытый скан
(разделение 90 вместо 210), а не краевой, и в комментарии так и сказано.

## 23. 19.09: `-r 5` по умолчанию — §15.1 отменён

`pb_redundancy=5` в `src/main.c`. Это разворот §15.1, и разворот по новым
данным, а не по передумалось.

§15.1 брал `-r 0` ради ёмкости: 1422 блока против 1180 на A4 при
`-d 150 --image-dpi 600`, то есть около 20%. Тогда это был размен ёмкости на
паритет при молчаливом допущении, что запас есть где-то ещё. §21 и §22
показали, что его нет: `-d 150` читается только с включённой чёткостью печати
(§21.2) и только при `-s 70` (§22.4). Два умолчания вместе — `-d 150` на
вершине узкого горба и `-r 0` без страховки — означали лист, который теряется
целиком от одной галки в чужом драйвере.

Замер на той же порче (полоса в 120 пикселей поперёк растра, `--dpi 100`,
30000 байт):

| | восстановлено | починено паритетом |
|---|---|---|
| `-r 5` (новое умолчание) | 334/334 | 40 |
| `-r 0` (прежнее) | 294/334 | 0 |

Цена прежняя, 20% байт на лист. Берётся потому, что умолчание должно
обслуживать лист, который не перепечатать, — а это ровно тот случай, для
которого паритет и существует. Кому нужна плотность, тот пишет `-r 0`
явно; кому нужна надёжность, тот раньше должен был знать, что её нет.

`-d` не тронут: 150 остаётся. `-d 120` по §22.5 выглядит лучше, но стоит 36%
ёмкости, а доказательств — один принтер и один сканер (§22.8).

Тесты: оба набора проходят без правок. Обе проверки про `-r` передают значение
явно — `-r 0` и `-r 5` — и от умолчания не зависели; граница ёмкости §15.1
привязана к `-r 5` с самого начала и теперь просто совпала с умолчанием.

## 24. 20.09: голосование между сканами — ядро отказов общее, но маленькое

Повод: в `experiments/` положен `BitReconstructor.BinVote` Артёма Дробанова
(VB.NET, Apache 2.0). Его `Vote()` — побитовое взвешенное большинство по N
побайтово выровненным копиям одного файла, минимум три входа. Вопрос: нужно ли
такое нам.

### Почему не в лоб

Голосование живёт на выровненных копиях. Выравнивание — это и есть работа
декодера; до неё голосовать не по чему, а после неё у нас уже есть два
инструмента сильнее:

* внутри блока RS(127,32) чинит до 16 битых байт **из одной копии**, плюс
  стирания с 32 известными позициями. Большинство по трём копиям чинит бит,
  только если большинство право;
* между копиями `Addblock` (`src/Fileproc.c:196`) берёт первую копию, прошедшую
  CRC, и остальные игнорирует. «Одно хорошее чтение побеждает» строго лучше
  «большинства из трёх»: CRC говорит, какое чтение хорошее, а голосование —
  инструмент для случая, когда отличить нельзя.

Остаётся одна дырка: блок, не прошедший CRC **ни в одном** скане, выбрасывается,
и между сканами о нём не помнится ничего. Окупится ли там объединение чтений,
решает одно — независимы ли ошибки. Это и померено.

### Замер

`experiments/vote_corr.py`: маски «клетка прочиталась неверно» считаются
`dot_diff`-ом на решётке **исходника**, поэтому два скана сравниваются клетка в
клетку. Лист `02-d150-s70`, клетка 4 px, 278 528 клеток.

| пара | A неверно | B неверно | неверно в обоих | при независимости | отношение | φ |
|---|---|---|---|---|---|---|
| два отпечатка, чёткость включена (run 3 / run 2) | 0.345% | 0.742% | 310 | 7.1 | **43x** | 0.216 |
| чёткость выключена / включена (run 1 / run 2) | 3.537% | 0.742% | 1513 | 73.1 | **20.7x** | 0.326 |
| один лист, 600 dpi / 300 dpi | 0.742% | 22.23% | 1362 | 459.8 | 3.0x | 0.091 |

Третья строка негодная и оставлена как отрицательный результат: на 300 dpi 128
тайлов из 272 просят сдвиг больше 6 px, подгонка на пределе, 22% — это уже не
измерение потерь, а шум.

Во второй строке `P(B неверно | A неверно) = 0.73`: ошибки хорошего листа почти
целиком подмножество ошибок плохого. Одни и те же точки тяжелы при любой
настройке драйвера — это та же систематика печати, что в §21 и §22.

### Вывод

Корреляция сильная, 20–43 раза против случая, но общее ядро мало: 310 клеток на
обоих листах против 2410, падающих ровно на одном. То есть большинство отказов —
свои у каждой копии, и третий читатель их снял бы. Только этот выигрыш уже взят
дешевле: при 0.3–0.7% на точку в блоке 32×32 выходит 3–7 неверных точек, RS
съедает до 16 байт, и блок читается с первой копии. Голосование окупается лишь
там, где один и тот же блок не читается во всех копиях, а это решает ядро —
0.11% клеток, на порядок ниже предела RS.

Порт BinVote ничего не добавит. Если когда-нибудь браться за ту дырку, то не
побитовым большинством, а усреднением уровней серого по сканам **до** порога:
полутон у нас на руках, `Recognizebits` и так перебирает пять порогов, а
голосование по уже пороговым битам выбрасывает ровно ту частичную информацию,
которой держится §20.

Чего набор не отвечает: чистой повторной оцифровки одного листа при одинаковых
600 dpi в нём нет, только 600 против 300. Независимость шума **сканера**
отдельно от печати не померена — это один лист, два скана, одинаковые
настройки, и та же таблица.

## 25. 20.09: окно адресов страницы, и erasures на всех девяти сетках

Пункт 1 из разбора Barroco (`analysis/COMPARISON_RU.md`): прежде чем ломать формат
ради CRC32, выбрать проверочные биты из уже имеющегося `addr`.

### 25.1 Что сделано

`Recognizebits`, `src/Decoder.c`. К проверке структуры адреса (§16.2 — полубайт
группы, кратность `NDATA`, `MAXSIZE`) добавлено окно адресов самой страницы:
если метка страницы уже прочитана, `offset` обязан лежать в
`[(page-1)*pagesize, page*pagesize)`.

Следом снят ограничитель §12: `Recognizebits(...,shiftorder[i]==4)` →
`(...,1)`, то есть erasures разрешены на всех девяти сдвинутых сетках, а не
только на несдвинутой.

Бюджет отбраковки поддельного блока, собранного erasure-декодом:

| проверка | бит |
|---|---:|
| CRC16 | 16 |
| полубайт группы ≤ NGROUPMAX | 0.7 |
| `offset % span == 0` | 6.5 |
| окно страницы (лист A4, 534 блока) | ~12 |
| итого | ~35 |

Было ~23 бита. Ровно эти ~12 бит и оплачивают восьмикратный рост числа попыток:
9 сеток × 534 блока ≈ 2¹² попыток на лист, то есть один ложный блок примерно на
2²³ листов против одного на 900 у §12.

### 25.2 Окно считается правильно — проверено на 7 страницах

Главный риск правки — ошибка в `(page-1)*pagesize`: тогда всё, кроме первой
страницы, отвергалось бы целиком. 288 000 байт, `-d 100`, 7 страниц:

| страница | блоков прочитано | отвергнуто окном |
|---|---:|---:|
| 1 | 490 | 0 |
| 4 | 490 | 0 |
| 7 | 260 | 0 |

Сборка всех семи — 3200/3200, SHA-256 совпал с исходником. Оба набора тестов
проходят под `make check`.

### 25.3 Выигрыша нет ни на синтетике, ни на бумаге

Сначала синтетика: лист `-d 150 --image-dpi 600`, скан через `experiments/scan.py`
(измеренная PSF, `ratio=2` → 2 px на точку), сдвиг фазы на полточки кропом 1 px,
`mtf` от 2.0 до 2.6. Двенадцать случаев: блоки и карта качества совпадают
посимвольно во всех двенадцати.

Затем шесть напечатанных листов §22, `print-set/scans3/`, `-s` по манифесту:

| лист | блоков, прежний код | блоков, девять сеток | erasure-декодов | принято через erasures |
|---|---:|---:|---:|---:|
| `01-d100-s70` | 223 | 223 | 105 | 0 |
| `02-d150-s70` | 223 | 223 | 7 132 | 4 |
| `03-d150-s100` | 221 | 221 | 31 293 | 5 |
| `04-d120-s70` | 223 | 223 | 113 | 0 |
| `05-d150-s50` | 141 | 141 | 140 516 | 15 |
| `06-d100-s50` | 223 | 223 | 1 245 | 12 |

Колонка блоков совпадает с §22.2 целиком. Код достигается — до 140 тысяч
erasure-декодов на лист, — и принятые через erasures блоки есть, но все до
одного пришли с несдвинутой сетки, которую прежний код и так пробовал.
Восемь дополнительных сеток не дали **ни одного** блока ни на одном листе.

Цена (тёплый кэш, по два повтора; первый прогон врал на 30 секунд — холодное
чтение 35-мегабайтного BMP, не декодер):

| лист | прежний код | девять сеток |
|---|---:|---:|
| `04-d120-s70` (читается) | 2.9 с | 2.9 с |
| `06-d100-s50` (читается) | 3.0 с | 2.9 с |
| `05-d150-s50` (82 блока потеряно) | 33 с | 47 с |

То есть читаемый лист не дорожает, а отказ — на 40%.

### 25.4 И один блок стал читаться хуже

На `03-d150-s100` карта качества разошлась в одной клетке из 223: прежний код
читал её обычным декодом с 6 исправлениями, новый — через erasures («+», то
есть 10 и больше). Блок тот же, счёт тот же, но доказательство слабее: обычное
чтение оставляло 20 байт запаса RS, erasure-чтение не оставляет ничего и висит
на CRC16.

Это и есть механизм, о котором предупреждал §12, только проявившийся не ложным
блоком, а подменой хорошего кандидата плохим: erasures на ранней сетке
срабатывают раньше, чем обычный декод на поздней.

### 25.5 Итог: окно оставлено, девять сеток отменены

`shiftorder[i]==4` возвращён. Ноль выигрыша на шести бумажных и двенадцати
синтетических листах, +40% на отказе и одно ослабленное чтение — этого хватает,
чтобы закрыть вопрос до появления листа, которому девять сеток нужны.

Окно адресов остаётся. Проверено на семи страницах (§25.2) и на всех шести
листах набора: колонка блоков совпала с §22.2 до единицы, `rejected_by_page_window`
равно нулю везде — законных блоков оно не режет. Польза у него своя: поддельный
блок из erasure-декода теперь стоит ~35 бит вместо ~23.

### 25.6 Что это говорит про CRC32

Мотив «CRC16 мешает восстановлению» проверен и не подтвердился. Он строился на
том, что §12 отключил восемь сеток из страха перед ложным принятием, — значит,
вернув биты, вернём и блоки. Биты вернули; блоков там не оказалось. Узкое место
не в ширине проверки, а в том, что erasures на сдвинутых сетках нечего читать:
если сдвинутая сетка годится для erasure-декода с проходом CRC, она обычно
годится и для обычного декода, разрешённого там с §8.

Пункт «CRC32» в списке ломающих формат изменений понижается до «нет доказанной
нужды». Из §12 при этом ничего не отменяется: ограничение осталось на месте,
просто теперь известно, что оно ничего не стоит.

## 26. 21.09: страница носит свой SHA-256, и разрыва формата не понадобилось

Последний уцелевший пункт разбора Barroco (`analysis/COMPARISON_RU.md`, п.1):
встроенный хеш исходного файла в машиночитаемых данных. CRC32 отпал в §25,
геометрия 4×2 не измерена — оставался только он.

### 26.1 Почему это не разрыв формата

Исходная посылка была неверна. Я считал, что хеш нужно объявить через байт
`mode`, а значит старые декодеры отвергнут лист. Проверил оба варианта
прототипом.

`mode=1`, текущий релизный бинарник:

```
Legacy compressed/encrypted pages are not supported
No readable file header found
```

Файл не создан. `Fileproc.c:93` отвергает любой `mode!=0`.

`mode=0`, хвост в данных, тот же бинарник:

```
Recovered 321/321 blocks; 0 missing; 0 repaired using redundancy
SHA-256 07379d...
байт в байт: True
```

Ни ошибки, ни предупреждения. Причина в том, что запас уже был: `Fileproc.c`
пишет ровно `origsize` байт, а `Printer.c` всегда округлял буфер до 16 —
то есть хранимый поток и раньше был длиннее файла, и всё за границей
отбрасывалось. Старый декодер не отличает 32 байта дайджеста от дополнения
и не должен.

Так что объявлять нечего: дайджест выдаёт зазор `datasize − origsize`. Без
него 0…15, с ним 32…47, диапазоны не пересекаются. И проверка
самодоказательна — случайных совпадений с SHA-256 не бывает.

`mode` остался нетронутым, под настоящий разрыв, когда такой понадобится.

### 26.2 Матрица совместимости, измеренная

| кодировал | читал | результат |
|---|---|---|
| 1.7 | 1.7 | 321/321, `matching the digest the page carries` |
| 1.7 | **1.6** | 321/321, байт в байт, без единого сообщения |
| 1.6 | 1.7 | 320/320, как раньше, дайджеста нет |

Цена: 321 блок против 320 на 28 800 байт. Один блок; на листе A4 — один из
примерно 1180.

### 26.3 Где лежит хеш и почему его не надо размазывать

Последние 32 байта хранимого потока, то есть один блок данных (изредка два,
если хвост пересекает границу) на последней странице. Отдельной защиты у него
нет — и не нужно: он едет в обычных блоках, под RS, CRC16 и XOR-паритетом,
наравне с данными.

Главное же в том, что `nblock = ceil(datasize/NDATA)` считает и его. Потерять
дайджест, не потеряв блок, нельзя, а блок не потеряешь незаметно. Дайджест
доступен ровно тогда, когда имеет смысл — на полном файле. Дублировать его по
страницам, как суперблок, значило бы защищаться от несуществующего случая.

### 26.4 Побочно: ложная тревога, которая была и до хеша

`incomplete` считался как `ndata != nblock`, по всем блокам. Блок, целиком
лежащий за `origsize`, содержит только дополнение — потеряв его, получаешь
идеальный файл и отчёт «повреждён».

С хвостом это стало бы заметно чаще: дополнение выросло с 0…15 байт до 32…47,
и доля файлов, у которых появляется блок целиком за `origsize`, поднимается
примерно с 17% до половины. Поэтому `incomplete` теперь спрашивает только про
байты, которые пишутся, а проверка `filecrc` — только когда прочитано всё.
Потеря блока с дайджестом даёт честное «файл цел, хеш не проверен».

### 26.5 Что это даёт и чего не даёт

Даёт: замену 16-битной `filecrc` — единственной автоматической проверки целого
файла — на 256-битную, которая едет вместе с листом. Блок, принятый на пределе
RS, ловился шестнадцатью битами; теперь двумястами пятьюдесятью шестью.

Не даёт: подлинности. Хеш подделывается вместе с данными и говорит только, что
восстановлено то же, что кодировалось. И ни одного потерянного блока он не
возвращает.

### 26.6 Проверки

`make check` и `make sanitize` (вариант с trap-on-error) проходят. Добавлено:
в `test_core.c` — совпадение, несовпадение с `--force` и без, и потеря блока за
`origsize`; в `test_cli.py` — строка про носимый дайджест в сквозном прогоне.

Версия поднята до 1.7: поведение видно снаружи, а по правилу §e7738e4 номер
должен описывать поведение.

## 27. 26.09: лист в PDF печатается в свой размер и чище, чем BMP

Вопрос: станет ли лист читаться лучше, если растрирует его сам принтер, а не
просмотрщик? Для этого `experiments/bmp2pdf.py` превращает страницу
кодировщика в PDF из двух 1-битных трафаретов (`/ImageMask`): точки и серый
текст шапки. У трафарета нет серых краёв, которые можно сгладить, при любом
масштабе. Подробности набора — в `pdf-set/MANIFEST.md`.

Страница одна на все листы: `print-set/02-d150-s70.bmp` (`-d 150 -s 70`,
600 dpi, 20000 байт), та же, что в §20–22. Принтер тот же, «улучшение
резкости» включено. Скан в оттенках серого, 600 dpi, без обработки.

### 27.1 Замер

Все листы: 223/223, без исправлений, байт в байт. Ошибки точек —
`dot_diff.py` против исходника, клетка 4 px:

| лист | чем печатался | неверных клеток | масштаб x / y |
|---|---|---|---|
| `01` BMP | мастер печати Windows, «по размеру кадра» **вкл** | 1016 (0.365%) | 0.969 / 0.972 |
| `01-nofit` BMP | тот же мастер, «по размеру кадра» выкл | 150 (0.054%) | 0.958 / 0.956 |
| `02` PDF, точки серые 0.25 | Acrobat, фактический размер | 1 (0.000%) | 0.999 / 0.995 |
| `03` PDF, точки чёрные | Acrobat, фактический размер | 0 (0.000%) | 0.999 / 0.994 |
| §22 `scans3/02` BMP | мастер, как в §20–22 | 962 (0.345%) | 0.972 / 0.973 |

Лист `01` повторяет §22 с разницей 0.02 п.п. и тем же масштабом. Значит, в
§20–22 печать шла с включённой галочкой «по размеру кадра».

### 27.2 Поправка к §20.2: 97% — это просмотрщик, не принтер

PDF из Acrobat в «фактическом размере» возвращается в 99.9% по x и 99.4–99.5%
по y. BMP из мастера печати в том же прогоне — 96.9% с галочкой и 95.8% без
неё. Лист ужимает путь печати BMP. Сам принтер печатает в размер, с
отклонением не больше 0.6%.

### 27.3 PDF чище BMP, и разница больше разброса

0 и 1 ошибка против 1016. Между одинаковыми листами разброс 0.3 п.п. (§20),
разница здесь 0.36 п.п. — больше, но не намного. Направление ясное; чтобы
считать его доказанным, нужны ещё листы.

Лист `01-nofit` (0.054%) показывает, откуда взялись потери BMP: из-за
пересчёта картинки под кадр, а не из-за самого принтера. Но и без галочки
масштаб остаётся 95.8%, так что BMP через мастер в размер не печатается никак.
Это один лист; повторять его не стали, раз PDF решает обе задачи сразу.

### 27.4 Серые точки против чёрных

Оба листа без ошибок. Чёрная точка на скане темнее (60 против 72 при бумаге
210–216), разделение 151 против 144. На глаз серый лист пятнистый: заливку
0.25 принтер кладёт полутоновым растром, и неравномерность тонера по листу
становится видна. На чтение это не повлияло, но запас у чёрной точки больше.

### 27.5 Что это меняет

- Печатать лист лучше из PDF в фактическом размере: масштаб правильный, потерь
  точек нет. BMP остаётся рабочим вариантом, `-d 150` читается и из него.
- Числа потерь в §20–22 включают пересчёт просмотрщиком. Выводы о плотностях
  там относительные и остаются в силе, но абсолютный уровень ошибок бумаги
  ниже, чем там записано.
- Печать из Chrome давала видимые пятна на сером листе и не сканировалась.
  Chrome, вероятно, растрирует PDF сам, так что замер относится только к
  Acrobat.

## 28. 26.09: `-o *.pdf` — вывод в PDF встроен в программу

По §27 лист лучше печатать из PDF, так что `bmp2pdf.py` переехал в
`Printer.c`. Расширение `-o` решает формат: `.pdf` (в любом регистре) — все
страницы в одном файле, иначе BMP по странице, как было.

Лист собирается тем же кодом, что и BMP: растр, шапка, подвал в одном буфере.
Вместо заголовка BMP из буфера пишутся две 1-битные маски (`/ImageMask`):
пиксели темнее 100 — точки, 100…199 — серый текст шапки. Точки печатаются
сплошным чёрным, текст серым 0.502, как в BMP. Флага для серых точек нет:
§27.4 показал, что чёрная точка даёт больше запаса.

### 28.1 Сжатие: RunLength, без zlib

Программа линкуется только с `-lm`, и так и осталось. На листе `02-d150-s70`
при 600 dpi, две маски вместе:

| сжатие | байт на страницу |
|---|---|
| без сжатия | 8.7 МБ |
| RunLength (фильтр PDF, ~20 строк кода) | 0.66 МБ, 0.70 с шапкой |
| Flate (`bmp2pdf.py`, через zlib) | 0.06 МБ |

0.7 МБ против 35 МБ у BMP той же страницы. Flate добавим, если размер начнёт
мешать; пока цена zlib на Windows больше выигрыша.

### 28.2 Ровно A4

Лист в пикселях округляется вверх: 210 мм при 600 dpi — это 4961 px,
595.32 pt при 595.28 у A4. `bmp2pdf.py` ставил страницу по пикселям, и
выходило на 0.014 мм больше A4. Теперь MediaBox — ровно размер бумаги
(`--paper`/`--paper-size`, в пунктах), а растр рисуется в своём истинном
масштабе от верхнего левого угла. Лишняя доля пикселя по правому и нижнему
краю — белое поле, и она обрезается. Масштаб точек не трогается.

### 28.3 Проверено

- Маски из PDF, распакованные `pypdf`, попиксельно совпадают с BMP той же
  страницы, с шапкой и без.
- PDF, отрендеренный MuPDF при 600 dpi без сглаживания, декодируется байт в
  байт: одна страница с шапкой и два листа случайных данных (150 000 байт).
- В `test_cli.py` добавлено: `-o norec.PDF` даёт один файл, `/Count` равен
  числу BMP-листов, MediaBox ровно A4, каждая запись xref указывает на свой
  объект, маска точек каждой страницы после своего RunLength-декодера совпадает
  с BMP. Если испортить счётчик повтора в кодере, проверка падает.
- `make check` проходит. Под Linux (WSL, gcc с `-Wall -Wextra -Werror`) —
  `test_core`, `test_cli.py`, `grid_check.py --check`; PDF создаётся с правами
  0600, как BMP.

На бумаге: один лист `input.bin` (20000 байт, `--image-dpi 600`, без шапки),
напечатанный из PDF самой программы, скан `D:\ScanImage96.bmp` (SHA-256
`2154a76b…0bba8`). Восстановлен байт в байт, 223/223 без исправлений, дайджест
страницы сошёлся. `dot_diff.py` против BMP той же страницы: 16 неверных клеток
из 348 160 (0.005%), тон точки 53 при бумаге 206 — уровень листов PDF из §27.

Одно отличие: масштаб 0.998 по x, но **0.976 по y**, тогда как листы §27 из
Acrobat пришли 0.994–0.995. Растр рисуется в истинном масштабе, так что
причина вне кода — печать или подача листа при скане; с одного листа не
различить. Декодер шаг сетки меряет сам, и на чтение это не повлияло.

Лист перепечатан тем же путём (Acrobat, фактический размер), скан
`D:\ScanImage97.bmp` (`53ef79ea…9e61d`): байт в байт, 223/223 без исправлений,
3 неверные клетки из 348 160 (0.001%), масштаб **0.999 / 0.996** — как в §27.
Сжатие по y не повторилось.

Чьё оно было, решила линейка. Растр этой страницы — 53,47 мм в высоту; на
отпечатке `96` он около 52 мм, то есть 97,3% ± 1% при точности линейки в
полмиллиметра. Скан дал 97,6%. Значит, лист ужала печать, сканер его
передал верно. Первой версией было обратное — что лист иначе лёг на стекло:
скан `96` сдвинут на 9 мм правее остальных (+210/+49 px против +64/−26 у
`97` и +57/−27 у §27). Сдвиг настоящий, но к масштабу отношения не имеет.

Итог по PDF из Acrobat: из трёх листов два пришли в 99,4–99,6% по y, один —
в 97,6%. Принтер изредка укорачивает лист по ходу бумаги, скорее всего на
подаче. Декодеру это безразлично — `96` прочитан с 16 ошибками точек из
348 160, — но линейкой мерить масштаб по одному листу нельзя.

Цифры в окне печати Acrobat («Документ: 209,9 × 296,7», ниже «209,97 ×
296,93») к этому не относятся. Нижние — A4 драйвера в целых точках 600 dpi,
4960 × 7014; верхние — округлённый вниз размер страницы, которая в PDF ровно
210 × 297 мм. Даже как настоящая, разница — 0.1%, вчетверо меньше, чем любой
замер здесь различает.

Версия поднята до 1.8: новое поведение видно снаружи.
